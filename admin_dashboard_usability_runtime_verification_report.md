# Admin Dashboard Usability Runtime Verification Report

## 1. Redline

- 命令：`bash scripts/scan-redline.sh special_cases_report.md`
- 结果：通过，退出码 `0`
- `BLOCKER=0`
- `REVIEW=1863`
- `ALLOWLIST=244`

## 2. 验证环境

- 页面：`http://127.0.0.1:8080/admin`
- 服务：本轮临时启动 `SERVER_PORT=8080 ./scripts/run-local-dev.sh`
- 运行库现状：
  - `/api/v1/admin/overview` 显示 `articleCount=2`、`sourceFileCount=4`
  - `/api/v1/admin/processing-tasks?limit=10` 显示 `runningCount=0`、`waitingCount=0`、`failedCount=0`、`succeededCount=4`
- 本次复跑与前一版 runtime 报告结论一致。
- 本轮未清库、未造数、未改代码。

## 3. 首页状态摘要是否已收层

是。

首屏状态摘要不再铺满所有治理卡片，而是收口为主卡 + 折叠区：

- 直接可见：`待人工确认草稿`、`知识条目`、`资料文件`、`答案反馈待处理`
- 折叠入口：`更多治理指标`

观察结果：`更多治理指标` 默认折叠，治理类卡片没有在首屏直接铺开。

## 4. 哪些主卡仍在首屏

首屏主卡保留为：

- `待人工确认草稿`
- `知识条目`
- `资料文件`
- `答案反馈待处理`

这四张卡都能在 DOM 快照中直接看到。

## 5. 治理类卡片是否已折叠

是。

展开 `更多治理指标` 后，能看到被降级的治理项：

- `资料源`
- `已确认修正`
- `待分析提问`
- `已入库待复核`
- `高风险内容`
- `热点待抽检`
- `用户反馈风险`

在未展开前，这些词都不在首屏状态摘要直接出现。

## 6. 当前处理任务是否不再展示多条完成记录

是。

当前为“无活跃任务”场景：

- 后端 API 返回完成任务共 `4` 条
- 页面“当前处理任务”区域只展示最新 `1` 条完成任务：`frontend-reject-source.md`

因此，页面层已按预期做了成功记录收口，没有把 4 条完成任务全部铺出来。

## 7. “任务线索”是否已移除

是。

页面正文与“当前处理任务”区域均未再出现 `任务线索` 文案。

## 8. 文案是否已人话化

大部分已完成：

- 未再看到 `反馈沉淀`
- 未再看到 `待处理反馈`
- 未再看到 `结果反馈待处理`
- 已看到替换后的：
  - `已确认修正`
  - `待分析提问`
  - `答案反馈待处理`
  - `质量检查`
- 未看到旧文案 `原因摘要`

但仍有一个残留：

- “待确认草稿”空详情提示仍显示：
  - `从左侧选择一条待确认草稿后，可查看正文、来源和 Reviewer 判定原因。`

这说明 `Reviewer` 仍然作为用户可见主文案残留在真实页面里，与 fix report 中“已改成人话”的目标不完全一致。

## 9. 是否仍有误导普通用户的地方

有，主要是上面的残留文案。

影响：

- 用户在“待确认草稿”空态下仍会看到 `Reviewer 判定原因`
- 这会把内部术语重新暴露出来，削弱“质量检查/人话文案”这一轮收口效果

除此之外，本轮没有看到新的误导性首屏问题：

- 首屏状态摘要层级明显比之前清楚
- “当前处理任务”不再把成功历史铺满
- `任务线索` 已移除

## 10. 是否建议提交

暂不建议直接提交。

阻塞原因：

- 真实页面仍残留 `Reviewer 判定原因` 文案，和本轮“用户主文案改成人话”的目标不完全一致

如果按严格验收口径，这一项应先做一个最小前端文案修正，再进入提交前质量复核。

## 11. 本轮是否修改代码

否。

本轮只做运行时验收与只读 API 校对；未修改任何代码、前端、测试或数据。
