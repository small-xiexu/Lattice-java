# 后台页面体验走查报告

## 总体判断

当前后台**可以用**，但还**不够适合普通非开发用户独立使用**。

优点已经很明确：

- 首页有状态摘要、帮助卡、当前处理任务、待人工确认草稿、已入库内容、结果反馈，主流程对象都在。
- `review_articles / fix_review_issues / generate_synthesis_artifacts / model_route` 这类内部黑话，默认界面基本已经收住。
- “待人工确认”面板已经能讲清“确认后才入库，驳回后不入库”，这是正确方向。

主要问题不在“功能缺失”，而在**信息分层还不够稳**：

- 首页卡片太多，且把 readiness、治理、风险、反馈混在一起。
- “当前处理任务”对普通用户仍偏工程化，信息层次重复。
- 部分状态语义仍可能误导用户对“是否已入库”的判断。

## 最影响体验的 5 个问题

### 1. 首页状态摘要卡片过多，且层级混杂

当前首页摘要有 10 张卡：

- 知识条目
- 资料文件
- 资料源
- 反馈沉淀
- 待处理反馈
- 需复核内容
- 待人工确认草稿
- 高风险内容
- 热点待抽检
- 用户反馈风险
- 结果反馈待处理

问题：

- 普通用户第一眼最关心的是“现在能不能提问”“有没有卡住的任务”“有没有待我确认的草稿”。
- 现在首页把治理、风险、反馈、覆盖、运营指标一次全摊开，用户要自己判断轻重缓急。

结论：**首页信息密度偏高，主次不够清楚。**

### 2. “待处理反馈”与“结果反馈待处理”语义太近，普通用户容易分不清

从文案看，这两张卡都像“有反馈没处理”：

- `待处理反馈`
- `结果反馈待处理`

再加一张：

- `反馈沉淀`

这三张卡对非开发用户都偏抽象。

问题：

- 用户很难知道这三个词分别对应什么对象。
- “反馈沉淀”是内部产品语言，不是用户语言。
- “待处理反馈”与“结果反馈待处理”需要明显区分“系统待修正”还是“用户提交的回答反馈”。

结论：**反馈类卡片当前最容易造成理解成本。**

### 3. “当前处理任务”里已完成 compile job 的文案，可能和人工确认最终结果冲突

当前 `processing-tasks` API 返回的样本里，4 条 compile job 全部显示：

- `status=SUCCEEDED`
- `displayStatusLabel=已完成`
- `reasonSummary=处理成功，资料已写入知识库`
- `progressSteps[REVIEW_ARTICLES].detail=质量检查后需要人工确认`

这里有明显的普通用户认知冲突：

- 一边说“处理成功，资料已写入知识库”
- 一边又说“质量检查后需要人工确认”

更关键的是，这些样本里包含 approve 和 reject 场景；对 reject 而言，“已写入知识库”从用户视角就是错的。

这不是前端术语问题，而是**当前处理任务卡片缺少“人工确认后的最终业务结果”字段**。

结论：**这是当前最容易误导用户的地方。**

### 4. “待人工确认”面板位置对已知用户可用，但对普通用户还不够显眼

优点：

- 面板标题直接叫“待人工确认”
- 副标题明确写了“确认后进入正式知识库，驳回后不会入库”
- 按钮文案“确认入库 / 驳回”也清楚

问题：

- 它被放在“当前处理任务”tab 里，而且位于任务面板之后。
- 用户如果只看首页摘要，不一定马上意识到入口在“当前处理任务”下面的第二块，而不是顶层独立入口。
- 列表项仍写 `Reviewer 判定需要人工确认`，这里的 `Reviewer` 对普通用户偏技术。

结论：**入口已经存在，但还不算“第一眼就懂”。**

### 5. 当前处理任务卡片信息重复，扫读成本偏高

任务卡当前可能同时出现：

- 标题
- 运行态 badge
- 摘要文案
- “下一步”提示
- 当前进度
- progress steps
- `任务线索`
- 失败面板

从前端实现看，`spotlightSummary`、`nextStepHint`、`reasonSummary`、`operationalNote` 会叠加展示。

问题：

- 对普通用户来说，“摘要 / 下一步 / 任务线索”三层很容易重复表达同一件事。
- 没有活跃任务时，页面仍会把最近完成任务提升到首页主板块，容易让首页显得一直很忙。

结论：**当前处理任务更像工程看板，不像普通用户工作台。**

## 哪些信息应保留在首页

建议保留在首页首屏的，应该只剩“做决定需要的信号”：

- 是否已经可用：帮助卡
- 当前是否有运行中 / 待确认 / 失败任务：处理任务摘要
- 待人工确认草稿：显式保留
- 知识条目
- 资料文件
- 结果反馈待处理：如果产品希望把问答闭环也放在首页，可保留

这些信息能回答三个核心问题：

1. 现在能不能直接提问
2. 有没有东西卡住我
3. 有没有必须人工确认的草稿

## 哪些信息应折叠到详情

建议从首页首屏降级到二级区块、详情页或治理页的有：

- 资料源
- 反馈沉淀
- 高风险内容
- 热点待抽检
- 用户反馈风险
- 需复核内容（如果它是已入库文章治理指标）
- 历史完成任务列表中的第 2~4 条
- 队列详情中的队列编号 / 编译任务 / 概念 / 文章键 / 审查路线 / 审查模型
- 当前处理任务里的 `任务线索`

原则：

- 能驱动“下一步动作”的留首页
- 只能解释历史、治理或技术上下文的收进详情

## 哪些内部节点名需要中文化

默认页面里以下内部节点名已经不该再直接出现，应继续保持隐藏：

- `review_articles`
- `fix_review_issues`
- `generate_synthesis_artifacts`
- `refresh_vector_index`
- `model_route`

仍然偏技术、建议中文化的人机界面用语：

- `Reviewer` -> `系统审查` 或 `质量检查`
- `反馈沉淀` -> `已确认修正`
- `待处理反馈` -> 需要改成更具体的对象名
- `结果反馈待处理` -> 需要改成更具体的对象名

如果未来必须把内部流程展示给用户，建议映射为：

- `review_articles` -> `质量检查`
- `fix_review_issues` -> `根据检查结果修正内容`
- `generate_synthesis_artifacts` -> `整理知识库概览`
- `refresh_vector_index` -> `更新检索索引`
- `compile_article_review_queue` -> `待人工确认草稿`

## 哪些问题只靠前端能改，哪些需要后端支持

### 纯前端文案 / 布局可解决

- 首页卡片过多，需要收层
- `Reviewer` 术语改成人话
- “反馈沉淀 / 待处理反馈 / 结果反馈待处理”三组文案收口
- 当前处理任务里重复文案减少
- 无活跃任务时，首页只保留一条最新完成任务提示

### 需要后端字段支持

- 人工确认后的 compile job 最终业务结果

原因：

当前 `processing-tasks` API 样本里，reject 后的 job 仍显示：

- `处理成功，资料已写入知识库`
- `写入知识库`

但 reject 的真实业务结果是“不入库”。

这类问题前端不能靠猜修，只能靠后端提供更明确的最终态字段，例如：

- `finalBusinessOutcome`
- `publishOutcome`
- `manualReviewResolution`

## 下一轮如果要修，只推荐一个最小前端优化范围

只推荐先做 **首页与当前处理任务的文案收口**，不碰编译流程、不碰人审逻辑。

最小前端范围：

- `src/main/resources/static/admin/modules/management-runtime-part-02.js`

只做三件事：

1. 首页状态摘要从 10 张卡压成“主卡 + 次卡”
2. 把 `Reviewer`、`反馈沉淀`、两类 `待处理反馈` 文案改成人话
3. 当前处理任务在无活跃任务时，减少完成任务展示数量，移除重复的 `任务线索`

原因：

- 这是纯前端可完成的体验收益点
- 不需要先改后端字段
- 能最快让首页从“工程看板”更像“普通用户工作台”

## 本轮是否修改代码

否。

本轮只做只读走查，基于页面源码、只读 API、现有运行时验证报告和页面结构进行分析；未修改任何前端、后端、测试、配置、脚本或数据库。
