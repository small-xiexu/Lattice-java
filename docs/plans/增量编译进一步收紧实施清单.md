# 增量编译进一步收紧实施清单

## 1. 目标

基于 `2026-04-22` 已将真实目录增量 `persistedCount` 从 `9` 收紧到 `4` 的结果，继续定位剩余 4 篇文章的来源，判断是否仍存在可进一步收紧的传播或聚合范围，完成：

- 查清真实增量作业中实际被刷新的是哪 4 篇文章
- 区分每篇文章属于“变更源文件直命中”“依赖传播命中”还是“聚合文章命中”
- 仅在不影响增量问答正确性的前提下，继续压缩不必要的文章刷新范围
- 形成新的真实回归结果与边界说明

## 2. 范围

- 文档台账：
  - `docs/plans/增量编译进一步收紧实施清单.md`
- 真实回归入口：
  - `docs/项目全流程真实验收手册.md`
  - `/tmp/lattice-real-e2e-20260418-src`
- 增量编译主链：
  - `src/main/java/com/xbk/lattice/compiler/service/IncrementalCompileService.java`
  - `src/main/java/com/xbk/lattice/compiler/graph/node/IngestSourcesNode.java`
- 依赖传播与持久化相关：
  - `src/main/java/com/xbk/lattice/governance/DependencyGraphService.java`
  - `src/main/java/com/xbk/lattice/governance/PropagationService.java`
  - `src/main/java/com/xbk/lattice/compiler/graph/node/PersistArticlesNode.java`
- 定向测试：
  - `src/test/java/com/xbk/lattice/compiler/service/IncrementalCompileServiceTests.java`
  - `src/test/java/com/xbk/lattice/compiler/service/StateGraphCompileOrchestratorTests.java`

## 3. 非目标

- 本轮不回退已经完成的“源文件差异筛选”方案
- 本轮不切换到 LLM 最终裁决方案
- 本轮不扩大到 repo rollback、历史快照 UI 或 Query 侧无关问题

## 4. 实施清单

- [x] N1 识别真实增量作业中被刷新的 4 篇文章
  - 验收：已从 `lattice.compile_job_steps` 确认增量作业 `1ca87935-8dcc-4a85-a4b9-04ee2e1c0652` 的 `persistedIds=[security, ops, integrations, fulfillment]`；并确认 `ingest_sources conceptCount=5`、`analyze_batches conceptCount=4`。

- [x] N2 判定 4 篇文章的命中来源与必要性
  - 验收：已确认 `ops` 对应真实变更 `ops/postmortem.md`；其余 `security / integrations / fulfillment` 并非真实业务变更，而是 PDF/Excel 的 `metadataJson` 在 `jsonb::text` 回读后带空格，和运行时 extractor 产出的紧凑 JSON 字符串不一致，造成伪变更命中。

- [x] N3 落地进一步收紧实现并补充测试
  - 验收：已将源文件差异筛选中的 `metadataJson` 比较改为 JSON 语义比较，并新增“metadata 仅格式差异不应命中变更”的单测；`IncrementalCompileServiceTests`、`StateGraphCompileOrchestratorTests`、`CompilePipelineVectorIndexingTests` 均通过。

- [x] N4 完成新的真实回归并回写结果
  - 验收：先对旧基线目录 `/tmp/lattice-real-e2e-20260422-baseline.9NOvdu` 执行全量（`jobId=1b31767c-b92a-45ed-8b8f-227b7e051bd5`，`persistedCount=9`），再对当前目录 `/tmp/lattice-real-e2e-20260418-src` 执行增量（`jobId=681e5f90-6895-40ff-bfc2-bca9cb3ab91d`，`persistedCount=1`，`persistedIds=[ops]`）；随后对同目录再次执行增量（`jobId=8b5e1a42-d4c5-498f-ab96-3a597c5e91c6`）得到 `persistedCount=0`，说明无变更重跑已幂等。

## 5. 验收口径

- 能明确列出真实增量作业里被刷新的文章名单与命中原因
- 若继续收紧，新的规则必须有对应测试覆盖
- 真实目录增量 `persistedCount` 若还能下降，必须附带新的真实作业结果
- 若不能再下降，也必须明确说明剩余刷新的 4 篇为什么是必要集合

## 6. 当前断点

- 当前阶段：本轮“进一步收紧”实施与真实回归已完成
- 已知事实：
  - 旧问题“整目录增量分析全部 `rawSources`”已修复
  - 当前真实增量结果为 `persistedCount=4`
- 下一步：若后续还要继续优化，只需要围绕真正的聚合文章策略做取舍；当前单文件变更已稳定收敛到 `persistedCount=1`，无变更重跑已收敛到 `0`。
