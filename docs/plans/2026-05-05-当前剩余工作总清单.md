# 当前剩余工作总清单

**文档版本**：v2.0  
**编写日期**：2026-05-05  
**适用仓库**：`/Users/sxie/xbk/Lattice-java`  
**定位**：本文是当前唯一活跃执行台账。后续计划推进、断点恢复、验收回写只更新本文。

---

## 0. 台账规则

- [x] 合并旧清单与旧方案中的待完成项
  - 状态：已完成
  - 验收：已把 Query 收口、Query 多通道并发召回、复杂文档结构化证据、人工复核闭环、风险抽检、结果反馈回流、工作台处理任务可见性、全面超越证据收口等未完成事项合并到本文。

- [x] 删除旧执行清单与旧方案源文件
  - 状态：已完成
  - 验收：旧文件已删除或保持删除状态；后续不再回写旧文档。

- 长期规则：后续开发只回写本文
  - 状态：长期执行，不纳入本轮完成率。
  - 要求：每完成一个可独立验收事项，立即把对应事项改为完成，并补测试、真实链路或人工核验依据。

### 0.1 长期参考资料

下面资料不是执行清单，不纳入删除范围：

- `docs/项目启动配置清单.md`
- `docs/项目全流程真实验收手册.md`
- `docs/卡券三期-迁移方案.md`
- `docs/数据库表结构详解.md`
- `docs/benchmark/*`
- `docs/test/*`
- `docs/ai agent/*`

### 0.2 状态约定

- `未开始`：尚未动工。
- `进行中`：已开始，需写明做到哪。
- `待验证`：实现完成但验收未完成。
- `阻塞`：无法继续，需写明原因和下一步。
- `已完成`：已实现且通过验收。

### 0.3 红线

- 不做旧逻辑兼容、不做灰度；当前项目按新项目重构。
- Query / 检索 / 回答主链禁止业务专名、特定文档、特定题号、特定样例字符串硬编码。
- 结构化资料问答只能基于通用 schema、字段、行、列、单元格与表级统计能力优化；禁止把 `scenario_id / case_num / step_index` 等样例列名做成业务特判。
- 风险治理不能退化成“用户必须全量审核知识条目”；人工复核只服务高风险、高价值、高不确定性与结果反馈。

---

## 1. 当前基础

- Query 侧：局部真实 Query 已能到 `generationMode=LLM / modelExecutionStatus=SUCCESS`，OCR 运行态问答已直读后台配置，运行态问答不再进入 pending，article 向量索引裁剪重试、ANN 自动建索引、`generate_synthesis_artifacts` 并发化、`response_format=json_schema`、若干 citation/fallback 收口已完成。
- 检索底座：article chunk lexical、source chunk lexical、refkey 独立权重、query rewrite、intent 分类、retrieval strategy、working set、retrieval audit、默认权重释放已完成一轮。
- 复杂文档侧：`fact_cards` 落库、生成、lexical/vector 检索、RRF 融合、review/fix、覆盖校验、后台统计/明细、query 过程记录、当前 15 题固定回归已跑通。
- 结构化资料侧：XLSX 行列问答暴露出精确行定位、字段抽取、聚合统计、引用行校验与长上下文截断问题；后续按通用结构化资料查询层收口，不针对单份文件或单个列名硬编码。
- 内容治理侧：`review_status=needs_human_review` 的展示、详情与基础治理入口已经存在，但确认通过、提交修正、风险维度、结果反馈与热点抽检还没闭环。
- 工作台侧：统一处理任务视图已有基础验证，但资料源详情联动、真实场景回归和手册口径仍需收口。

### 1.1 本期 P0 执行边界

本期只把“通用结构化资料问答 MVP”做到可验证，不把整份路线图都纳入本轮完成口径。

- [x] P0-1：完成通用表格行列入库 MVP
  - 状态：已完成
  - 范围：XLSX / CSV；保留 source、sheet/table、row、column、cell、normalized value、row text。
  - 不含：JSON/YAML、复杂跨表 join、排序高级语义、后台 UI。
  - 已完成：已确认 Excel 当前只生成带表头的纯文本行，`structuredContentJson` 通道已存在但未承载表格结构。
  - 验收：已新增 XLSX / CSV 通用结构化 JSON 抽取、`structured_tables / structured_table_rows / structured_table_cells` 入库路径，并通过 `StructuredTableJdbcRepositoryTests,DocumentParseRouterIntegrationTests`。
  - 下一步：进入 P0-2 结构化查询 MVP。

- [x] P0-2：完成结构化查询 MVP
  - 状态：已完成
  - 范围：字段等值过滤、字段投影、count、group by、两行对比的确定性查询计划。
  - 验收：LLM 不负责数数，不从大文章里猜字段值。
  - 已完成：字段等值过滤、字段投影、count、group by、两行对比已接入结构化查询短路。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dtest=StructuredQueryPlannerTests,StructuredTableJdbcRepositoryTests,DocumentParseRouterIntegrationTests,QueryControllerTests test`，19 个测试通过。
  - 下一步：进入 P0-3 行 / 单元格证据与校验 MVP。

- [x] P0-3：完成行 / 单元格证据与校验 MVP
  - 状态：已完成
  - 范围：答案引用能落到 source + sheet/table + row + column；字段值和聚合结果可复核。
  - 验收：目标行不匹配、字段值不匹配、聚合不可复算时不得通过。
  - 已完成：Query 响应新增 `structuredEvidence`，包含 `queryType`、行证据、单元格证据、聚合证据；结构化短路内已做过滤行、投影字段、分组聚合最小一致性校验。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dtest=StructuredQueryPlannerTests,StructuredTableJdbcRepositoryTests,DocumentParseRouterIntegrationTests,QueryControllerTests test`，19 个测试通过。
  - 下一步：进入 P0-4 结构化资料固定回归。

- [x] P0-4：完成结构化资料固定回归
  - 状态：已完成
  - 范围：10-20 题，覆盖精确字段、行定位、枚举统计、两行对比、步骤明细、无命中。
  - 门槛：精确字段答案准确率 `>= 90%`；来源行正确率 `>= 95%`；聚合可复算率 `100%`；单题接口超时率 `<= 5%`；结构化题 fallback 率 `<= 10%`。
  - 已完成：新增 `StructuredTableQueryRegressionTests`，固定 10 题覆盖 CSV/XLSX 精确字段、行定位、count、group by、两行对比、步骤明细、无命中。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dtest=StructuredQueryPlannerTests,StructuredTableJdbcRepositoryTests,DocumentParseRouterIntegrationTests,QueryControllerTests,StructuredTableQueryRegressionTests test`，21 个测试通过。

---

## 2. 主线 A：Query 主链、Compile 基线与真实验收

### A1. 普通 Query 稳定到主链默认态

- [x] 建立固定真实 Query 回归集
  - 状态：已完成
  - 收口口径：本项已按末尾验收依据完成；下方早期 `进行中 / 待修` 记录仅保留为排查历史，不再代表当前阻塞状态。
  - 范围：入口方式、配置解释、架构原因、运行态说明、字段名、状态码、枚举、配置键、Excel 行列、PDF 表格、复杂规则、Deep Research 多跳题。
  - 验收：每题记录 `queryId`、`generationMode`、`modelExecutionStatus`、`reviewStatus`、`answerOutcome`、citation coverage、命中来源与人工判定；本期 P0 结构化题按 `1.1` 门槛判定。
  - 进行中：用户反馈接口解释题 `/api/v2/fulfillment/request/return 这个是什么接口呢` 的答案混入同段状态结论与补充事实，正在复验回答准确性、召回证据与最终答案组织链路。
  - 进行中：正在补固定题集与可复跑 runner/gate，先让回归记录口径标准化，再继续压降 LLM/SUCCESS 和 citation 误降级。
  - 历史记录：此前 `lattice` schema 曾因 Flyway V1 checksum mismatch 无法启动；当前已废弃 Flyway，改由 `src/main/resources/db/schema.sql` 手动重建唯一 `lattice` schema。
  - 历史记录：此前重建空库后曾被 `deep_research` 场景缺少启用中的 `agent_model_bindings` 拦截；当前启动口径以唯一 `lattice` schema + 手动 DDL 为准。
  - 进行中：已按 `.claude/t1.md` 恢复通用模型中心配置，`compile/query/deep_research` 绑定和 embedding profile 已写入空库；DeepSeek chat、Mimo chat、智谱 embedding 最小模型探测均通过，下一步重启正常校验模式并导入 `docs/scenarios.xlsx`。
  - 进行中：18082 已在正常启动校验模式下启动成功，`DeepResearchBindingValidator` 已通过；下一步通过后台编译上传接口导入 `docs/scenarios.xlsx`。
  - 进行中：已通过 `/api/v1/admin/compile/upload` 提交 `docs/scenarios.xlsx`，jobId=`e408b283-bae2-41d6-94b6-29c077a7f8b1`；结构化表已入库 `2` 张表 / `1542` 行 / `15069` 单元格，当前仍在 `compile_new_articles` 生成文章草稿。
  - 进行中：临时结构化回归显示 `GROUP_BY` 已命中 `RULE_BASED/STRUCTURED_QUERY` 且 25ms 返回，但“字段=值行定位/字段投影”前两题未进入结构化短路，落入普通 Query 图后超时；下一步定位通用 Planner/字段解析/结构化来源引用，不做任何业务字段特判。
  - 已完成：`docs/scenarios.xlsx` 编译 jobId=`e408b283-bae2-41d6-94b6-29c077a7f8b1` 已 `SUCCEEDED / finalize_job / 55/55`；结构化表计数为 `2` 表 / `1542` 行 / `15069` 单元格。
  - 已完成：结构化短路已按通用列名归一化、自然语言投影、两行对比、结构化证据 coverage 口径收口；未对 `scenario_id / case_num / step_index` 或 `scenarios.xlsx` 做主链特判。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=StructuredQueryPlannerTests,QueryControllerTests,StructuredTableQueryRegressionTests test`，`StructuredQueryPlannerTests=6`、`QueryControllerTests=12`、`StructuredTableQueryRegressionTests=1` 全部通过。
  - 验收依据：18082 真实结构化 4 题回归 `.codex/run/query-regression-structured-only-after-renderer-fix-20260506/query_summary.tsv` 全部 PASS；行定位、字段投影、group by、两行对比均为 `RULE_BASED / SKIPPED / STRUCTURED_QUERY`，`casePassRate=1`、`timeoutRate=0`、`averageCitationCoverage=1`。
  - 验收依据：防特判扫描 `rg -n "scenario_id|case_num|step_index|scenarios\\.xlsx|100912|100714|100716|场景用例" src/main/java scripts --glob '!target/**'` 仅命中守门脚本 `BLOCKED_PATTERN`，未命中生产实现。
  - 进行中：完整 `docs/test/query-regression-suite.json` 已跑完一次，输出目录 `.codex/run/query-regression-full-after-structured-fix-20260506`；10 题中 4 PASS，`casePassRate=0.4`、`timeoutRate=0.3`、`averageCitationCoverage=0.6`。因当前库只导入了 `docs/scenarios.xlsx`，接口迁移、MQ、项目本地配置等非 scenarios 题缺少语料，不能作为完整语料准确率门槛。
  - 待修：完整回归里 `Q-RUNTIME-OCR-001` 属于 runner 未把 `RULE_BASED / SKIPPED` 且有 sources 的确定性证据计入 coverage；`Q-STRUCT-ROW-001` 属于旧断言仍要求中文“备注”；`Q-NO-HIT-001` 属于真实无命中保护问题，精确不存在标识不应从无关证据生成成功答案。
  - 已完成：通用 no-hit 精确标识保护已收口；带 `_ / - / = / .` 等精确 token 的问题在证据未覆盖该 token 时不会保留弱相关命中，也不会把无关 scenarios 证据带入答案。
  - 已完成：runner 已把 `RULE_BASED / SKIPPED` 且存在 sources 的确定性运行态答案计入有效 coverage；负向 no-hit 题允许 `minSourceCount=0 / minCitationCoverage=0`，不再要求给不存在知识补引用。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=QueryGraphOrchestratorTests,AnswerGenerationServiceTests,QueryControllerTests,StructuredQueryPlannerTests,StructuredTableQueryRegressionTests test`，77 个测试通过。
  - 验收依据：18082 小回归 `.codex/run/query-regression-focused-after-nohit-fix-20260506-3/query_summary.tsv` 三题全 PASS；`Q-RUNTIME-OCR-001` coverage=1，`Q-STRUCT-ROW-001` coverage=1，`Q-NO-HIT-001` 返回 `NO_RELEVANT_KNOWLEDGE / RULE_BASED / SKIPPED` 且 sources=0。
  - 已完成：精确 no-hit 已不再误答，且通用检索耗时已收敛；强标识符问题的 lexical 检索只以强标识 token 作为宽文本锚点，同时修正 SQL LIKE 中 `_ / %` 被当通配符的问题，未对字段、文档或样例做特判。
  - 验收依据：18082 小回归 `.codex/run/query-regression-focused-after-nohit-fix-20260506-5/query_summary.tsv` 三题全 PASS；`Q-NO-HIT-001` 从约 12082ms 降至 612ms，返回 `NO_RELEVANT_KNOWLEDGE / RULE_BASED / SKIPPED` 且 sources=0。
  - 进行中：完整回归 `.codex/run/query-regression-full-after-nohit-latency-fix-20260506/query_summary.tsv` 为 10 题 7 PASS；结构化 5 题、运行态题、no-hit 均通过，但 `Q-CONFIG-001` 暴露“答案复述问题词导致 runner 假通过、弱相关 scenarios 证据被 fallback 采纳”的通用问题。下一步先收紧 runner 的答案术语判定，再修弱相关证据过滤。
  - 已完成：runner 已在 required answer terms 判定前剔除问题复述段；答案 fallback 已增加通用多技术焦点覆盖校验，防止无关 `key=value` 片段被当作 PostgreSQL/Redis 等多焦点配置题的直接证据。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AnswerGenerationServiceTests,QueryControllerTests,QueryGraphOrchestratorTests test` 通过；18082 单题回归 `.codex/run/query-regression-config-only-after-facet-guard-20260506/query_summary.tsv` 不再假 PASS，返回 `INSUFFICIENT_EVIDENCE / LLM / SUCCESS`，未再把 `scenarios.xlsx` 的折扣变量误答为数据库配置。
  - 已完成：多技术焦点/配置类问题的通用检索降耗已收口；lexical 检索在问题包含多个 ASCII/技术焦点 token 时优先使用这些 token 作为宽文本锚点，避免中文泛词 LIKE 扫描，未对具体资料、字段或样例值做特判。
  - 验收依据：18082 单题回归 `.codex/run/query-regression-config-only-after-tech-focus-20260506/query_summary.tsv` 中 `Q-CONFIG-001` 从约 98728ms 降至 1753ms；当前库缺少配置资料时返回 `NO_RELEVANT_KNOWLEDGE / RULE_BASED / SKIPPED`、sources=0，未再把 `scenarios.xlsx` 误答为数据库配置。
  - 验收依据：18082 小回归 `.codex/run/query-regression-focused-after-nohit-fix-20260506-6/query_summary.tsv` 三题全 PASS；运行态 77ms、结构化行定位 72ms、no-hit 711ms。
  - 已完成：answer prompt 证据上下文已做通用预算治理；按 evidence section 限制命中数，按单条 content / metadata 限长，优先保留贴题 fact snippet 与短上下文，避免把完整长正文交给 `LlmGateway` 总入口兜底截断。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AnswerGenerationServiceTests,QueryPreparationServiceTests,SourceSearchServiceTests,QueryControllerTests,QueryGraphOrchestratorTests test` 通过；新增 `shouldBoundLongEvidenceContentInAnswerPrompt` 验证长证据正文不会完整进入 answer prompt。
  - 验收依据：18082 小回归 `.codex/run/query-regression-focused-after-prompt-budget-20260506/query_summary.tsv` 三题全 PASS；配置单题 `.codex/run/query-regression-config-only-after-prompt-budget-20260506/query_summary.tsv` 1035ms 返回 `NO_RELEVANT_KNOWLEDGE / RULE_BASED / SKIPPED`、sources=0，缺语料时未误答。
  - 已完成：完整 Query 回归重新分类；`.codex/run/query-regression-full-after-prompt-budget-20260506/query_summary.tsv` 为 10 题 6 PASS，HTTP 失败率 0、超时率 0，结构化 5 题、运行态、no-hit 均通过；失败的 path / MQ / config / Deep 题均在当前仅导入 `scenarios.xlsx` 的语料边界下返回缺证据或无知识，没有再把无关 scenarios 证据误答。
  - 进行中：已确认剩余 4 题所需语料在仓库内存在：`docs/项目启动配置清单.md` 覆盖 PostgreSQL/Redis，`docs/卡券三期-迁移方案.md` 覆盖接口 path、MQ 边界和 FC/DPFM 边界；下一步导入这两份资料后复跑完整套件，而不是在 Query 主链为这些问题写特判。
  - 已完成：已通过 `/api/v1/admin/compile/upload` 同步导入上述两份资料，jobId=`43820e66-291a-4dba-9ce3-70cf2e604980`；编译 `SUCCEEDED / finalize_job / 23/23`，耗时约 15 分 42 秒，`persistedCount=23`，当前库 `source_files=3`、`articles=78`、`source_file_chunks=2575`、`fact_cards=1022`。
  - 已完成：补齐语料后已复跑完整 Query 回归；`.codex/run/query-regression-full-after-docs-import-20260506/query_summary.tsv` 为 10 题 9 PASS，HTTP 失败率 0、超时率 0，结构化 5 题、运行态、MQ、config、Deep、no-hit 均通过。
  - 待修：剩余 `Q-EXACT-PATH-001` 命中来源和引用覆盖正确，但最终因 `LLM_OUTPUT_INVALID` 进入 deterministic fallback 后，被通用“多路径列表”候选抢占，结论漏掉 `path` 约束口径并混入相邻路径；下一步修通用精确标识 fallback 的“显式标识符优先 + 问题维度覆盖”，不为具体接口或资料写特判。
  - 进行中：通用精确路径题第一轮修复已落地；裸写 URL/API path 已纳入通用显式标识符提取，路径信号从“任意 /”收紧为真实 path/HTTP method，deterministic fallback 对“显式标识符 + path 规则/变更问题”优先覆盖被点名路径并补足约束行，同时非 JSON 但带源文件引用且覆盖精确路径/约束维度的 Markdown 可保留为 `LLM/SUCCESS`。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AnswerGenerationServiceTests test` 通过；新增 `shouldPreferRequestedPathAndConstraintInFallbackAnswer`、`shouldReuseGroundedMarkdownForExactPathQuestion`，未引入具体文档、接口或样例列名特判。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AnswerGenerationServiceTests,QueryPreparationServiceTests,SourceSearchServiceTests,QueryControllerTests,QueryGraphOrchestratorTests test` 通过。
  - 已完成：精确路径 deterministic fallback 的相邻路径抢占已按通用规则修正；真实 `Q-EXACT-PATH-001` 已返回被点名的 `POST /api/v2/fulfillment/request/return`、一次性支付退款语义与“DPFM 入口 API path / 出入参对齐 FC 原接口”的约束，不再混入相邻 `refund/check` 或批量接口结论。
  - 验收依据：新增通用匿名回归 `shouldPreferRequestedPathContractAcrossMixedFusedHits`，覆盖真实 fused hits 形态下的“点名路径 + 相邻路径列表 + path 契约补充证据”；`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AnswerGenerationServiceTests,QueryPreparationServiceTests,SourceSearchServiceTests,QueryControllerTests,QueryGraphOrchestratorTests,CitationExtractorTests,CitationCheckServiceTests,DeepResearchResearcherServiceTests test` 通过。
  - 验收依据：18082 完整回归 `.codex/run/query-regression-full-after-path-contract-ranking-fix-20260506/query_summary.tsv` 为 10 题 9 PASS，HTTP 失败率 0、超时率 0；结构化 5 题、运行态、MQ、config、Deep、no-hit 均通过，`Q-EXACT-PATH-001` 内容命中 required terms 且 citationCoverage=1。
  - 待修：`Q-EXACT-PATH-001` 仍因精确查值保护返回 `FALLBACK / DEGRADED / DETERMINISTIC_EXACT_LOOKUP_PREFERRED`，导致固定 suite 的 `LLM/SUCCESS` 与整体 `llmSuccessRate>=0.4` 门槛未过；下一步追模型结构化答案为何被 `preferDeterministicExactLookupPayload` 打回，而不是改 gate 或硬改状态。
  - 进行中：正在追 `preferDeterministicExactLookupPayload` 的通用 grounding 判定与模型输出解析路径，目标是在答案覆盖显式 path 与 path 契约时保留 `LLM / SUCCESS`。
  - 进行中：已补通用“显式标识符问题不复述未点名 path 反例”约束与匿名 demo path 回归，聚焦 `AnswerGenerationServiceTests` 三题通过；下一步跑 Query 相关套件并重启 18082 验证 `Q-EXACT-PATH-001`。
  - 已完成：精确 path 题已从 `FALLBACK / DEGRADED` 收口为真实链路 `LLM / SUCCESS`；模型复述未点名反例 path 的问题已按通用显式标识符规则清理，未对具体接口、文档、字段或样例写特判。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AnswerGenerationServiceTests,QueryPreparationServiceTests,SourceSearchServiceTests,QueryControllerTests,QueryGraphOrchestratorTests,CitationExtractorTests,CitationCheckServiceTests,DeepResearchResearcherServiceTests test` 通过。
  - 验收依据：18082 单题 `/api/v2/fulfillment/request/return 这个是什么接口？它迁移后对外 path 可以改吗？` 返回 `queryId=efe54760-bf13-42da-bee3-44117b124286`，`SUCCESS / LLM / SUCCESS`，无 fallback，答案未复述未点名反例 path。
  - 验收依据：18082 完整回归 `.codex/run/query-regression-full-after-exact-path-unrequested-path-cleanup-20260506/query_metrics.json` 为 `10/10 PASS`，`casePassRate=1`、`httpFailureRate=0`、`timeoutRate=0`、`fallbackRate=0`、`llmSuccessRate=0.4`、`averageCitationCoverage=0.8202`；`Q-EXACT-PATH-001` 为 `LLM / SUCCESS` 且 `citationCoverage=0.6`。
  - 验收依据：防特判扫描 `rg -n "新路径|cleanupConclusionEvidenceLine|scenario_id|case_num|step_index|scenarios\\.xlsx|100912|100714|100716|场景用例" src/main/java scripts --glob '!target/**'` 仅命中守门脚本 `scripts/run-b1-structured-regression.sh` 的 `BLOCKED_PATTERN`，未命中生产实现。
  - 进行中：三资料真实验收 `.codex/run/answer-accuracy-20260506230844/results.jsonl` 当前 3/8 PASS；待修项集中在聚合答案漏总数、接口 path 漏 HTTP method、PDF 普通资料题被结构化空结果截断、PDF 证据召回与长问答超时。下一步按通用结构化查询、显式标识证据补强、source chunk 检索和答案预算治理修复，禁止对具体资料或题目加特判。
  - 进行中：用户确认不接受为当前文档/题集精修的特殊逻辑；已撤回本轮 `QueryTokenExtractor` 中新增的 token 预算、中文问法清洗和均衡抽样方向。下一步只允许推进结构化入库、通用检索执行保护、通用证据扩展和通用答案校验，不再改 Query 主链去适配具体问法或词语。
  - 已完成：清理本轮新增的问法词/语境词驱动逻辑；`QueryIntentClassifier` 不再通过“权威配置/硬编码/如何看待/第几批”等词把问题强行归类，`AnswerShapeClassifier` 不再保留强 policy 问法分支，`AnswerGenerationService` 不再通过“权威/硬编码/迁移后/新增值/批次范围”等词加权或决定固定结论条数。保留的能力仅限通用结构形态：精确标识符覆盖、path/URL 语法、HTTP method + path、结构化表格聚合、citation 与 source/fact card 引用归位。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -DskipTests compile` 通过；`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AnswerShapeClassifierTests,AnswerGenerationServiceTests,QueryTokenExtractorTests,StructuredQueryPlannerTests,StructuredTableQueryRegressionTests test` 通过。
  - 验收依据：防特判扫描 `rg -n "scenario_id|case_num|step_index|scenarios\\.xlsx|100912|100978|100979|100914|美团|充电宝|卡券三期|/api/v2/fulfillment/request/return|featureToggleList|权威配置|硬编码|新增值" src/main/java src/main/resources/static/admin scripts --glob '!target/**'` 仅命中守门脚本 `BLOCKED_PATTERN` 与无关模型配置注释，未命中 Query / 检索 / 回答主链实现。

- [x] 把普通 Query 的 `LLM/SUCCESS` 从单题跑通提升到默认稳定
  - 状态：已完成
  - 当前：固定真实 Query 回归集中需要 LLM 生成的 path / MQ / config / Deep 题均为 `LLM / SUCCESS`，结构化、运行态和 no-hit 题按设计走 `RULE_BASED / SKIPPED`。
  - 验收依据：18082 完整回归 `.codex/run/query-regression-full-after-exact-path-unrequested-path-cleanup-20260506/query_metrics.json` 为 `10/10 PASS`，`fallbackRate=0`、`llmSuccessRate=0.4`，达到当前 gate。

- [x] 继续压降 citation 二次检查和 fallback 误降级
  - 状态：已完成
  - 当前：固定真实 Query 回归未再出现 `citation_check / finalize` 把成功答案打回 `FALLBACK / DEGRADED` 的 case。
  - 验收依据：18082 完整回归 `.codex/run/query-regression-full-after-exact-path-unrequested-path-cleanup-20260506/query_summary.tsv` 中 10 题 `fallback_reason` 均为空或设计内 `STRUCTURED_QUERY`，`fallbackRate=0`；`Q-EXACT-PATH-001` 为 `LLM / SUCCESS` 且 `citationCoverage=0.6`。

- [x] 继续打磨精确标识题的通用处理
  - 状态：已完成
  - 当前：本期固定集内的结构化行定位、字段投影、聚合统计、两行对比、配置键解释、显式 path 解释均已按通用链路通过；表格行列题优先走结构化查询计划，path 题按显式标识符与契约语义收口。
  - 依赖：表格行列题、精确字段题、聚合统计题依赖 `B0 / 本期 P0` 的结构化查询 MVP；本期依赖已满足。
  - 验收依据：18082 完整回归 `.codex/run/query-regression-full-after-exact-path-unrequested-path-cleanup-20260506/query_summary.tsv` 中 `Q-STRUCT-ROW-001 / Q-STRUCT-PROJECTION-001 / Q-STRUCT-AGG-001 / Q-STRUCT-COMPARE-001 / Q-CONFIG-001 / Q-EXACT-PATH-001` 全部 PASS。
  - 验收依据：防特判扫描仅命中守门脚本 `BLOCKED_PATTERN`，未命中生产实现；未对 `scenario_id / case_num / step_index`、具体文档或具体接口 path 写主链特判。

- [x] 继续收敛 `deep_research` 退化路径
  - 状态：已完成
  - 当前：解释型问题可进入 `deep_research`，researcher 层已对任务级检索异常、答案生成异常和 schema repair 异常做局部降级。
  - 已完成：`DeepResearchResearcherService` 增加 `retrieval_failed / answer_generation_failed / schema_repair_failed` 等通用 gap，并在答案生成异常时回退到已有检索证据生成最小 finding / anchor，避免单 task 抛出拖垮整轮。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=DeepResearchResearcherServiceTests test`，13 个测试通过；本轮防特判扫描未命中。

- [x] 把结构化输出从 `json_object` 升级到 `json_schema`
  - 状态：已完成
  - 当前：结构化问答和审查重写用途自动注入严格 `json_schema`，约束 `answerMarkdown / answerOutcome / answerCacheable`、枚举值与 `additionalProperties=false`。
  - 已完成：确认 Spring AI `1.1.2` 本地 `ResponseFormat` 支持 `JSON_SCHEMA`；`LlmInvocationExecutor` 已从 JSON mode 升级为 Structured Outputs schema；`ChatClientRegistry` 显式 schema 透传已补回归。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=LlmInvocationExecutorTests,ChatClientRegistryTests test`，11 个测试通过；本轮防特判扫描未命中。

- [x] 为 OCR / 运行态问答补独立测试
  - 状态：已完成
  - 验收：至少覆盖 `queryId=null`、不会新增 pending、直读后台真实配置。
  - 已完成：`QueryControllerTests.shouldAnswerOcrRuntimeStatusWithoutPendingQuery` 覆盖普通问答入口命中 OCR / 文档识别运行态问题时直读后台连接与默认路由配置，返回 `RULE_BASED / SKIPPED`，`queryId=null` 且不新增 pending。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=QueryControllerTests test`，10 个测试通过；本轮防特判扫描未命中主链和测试样本。

- [x] 把 `annIndexReady=true` 纳入向量重建回归
  - 状态：已完成
  - 验收：向量重建后同时确认 `indexedArticleCount`、`annIndexType`、`annIndexReady`。
  - 已完成：`AdminVectorIndexControllerTests` 在普通重建与维度对齐重建后均追加状态复查，断言 `indexedArticleCount=1`、`annIndexReady=true`、`annIndexType` 非空。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AdminVectorIndexControllerTests test`，3 个测试通过；本轮防特判扫描未命中。

### A2. 入库准确性与 Compile 性能基线

- [x] 固定入库准确性回归样本集
  - 状态：已完成
  - 范围：Markdown、DOCX、XLSX、PDF、HTML、`docs/test`、`deep_evals`、AI Agent PDF、公司业务 PDF。
  - 验收：持续核对文章正文、摘要、`reviewStatus`、source chunk、article chunk、向量状态、关键事实抽取完整度。
  - 进行中：A1 固定 Query 回归已收口；下一步梳理现有 compile/document parse 测试与样本目录，补最小可复跑入库准确性样本集，不把问答命中当作入库准确率。
  - 已完成：新增固定入库准确性样本回归 `DocumentParseRouterIntegrationTests.shouldPersistFixedIngestionAccuracySampleSet`，覆盖 Markdown、HTML、CSV、XLSX、文本型 PDF、DOCX，经 `ingest -> persistSourceFiles -> persistSourceFileChunks` 真实入口校验 `source_files` 正文/metadata、`source_file_chunks`、`structured_tables / rows / cells`。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=DocumentParseRouterIntegrationTests test` 通过。

- [x] 补一轮新的真实 compile 基线
  - 状态：已完成
  - 当前：旧基线不能代表当前代码口径。
  - 下一步：在稳定实例上复跑同一批样本，保留 `jobId / startedAt / finishedAt / duration / persistedCount`，形成优化前后对比。
  - 待纳入：本轮导入 `docs/卡券三期-迁移方案.md` 时，`compile-article` 多次触发 `LlmGateway` 输入截断，后续 compile 基线需要记录长文切分、单篇 prompt 预算和 Writer 调用耗时。
  - 进行中：18082 当前已稳定运行；下一步准备临时真实样本目录并通过真实 HTTP compile 入口采集 `jobId / startedAt / finishedAt / duration / persistedCount` 与入库计数。
  - 已完成：已用临时真实样本 `/tmp/lattice-compile-baseline-20260506` 通过 18082 管理侧 HTTP 入口 `POST /api/v1/admin/compile/jobs` 跑完一轮 `state_graph` full compile。
  - 验收依据：产物已保存到 `.codex/run/compile-baseline-20260506/`；`jobId=789ebfd2-f724-4dab-b9be-f8b52bea8694`，`status=SUCCEEDED`，`startedAt=2026-05-06T09:20:32.823605Z`，`finishedAt=2026-05-06T09:22:27.531685Z`，`duration=114.708s`，`persistedCount=3`。
  - 验收依据：样本入库计数为 `source_files=3`、`source_file_chunks=3`、`articles=3`、`article_chunks=3`、`fact_cards=2`；节点耗时记录在 `.codex/run/compile-baseline-20260506/step_durations.tsv`。
  - 后续慢点：本轮 `compile_new_articles=51.462s`、`generate_synthesis_artifacts=60.945s`，下一项优先围绕这两段量化和收口。

- [x] 收口 `compile_new_articles / review_articles / fix_review_issues` 长耗时
  - 状态：已完成
  - 当前：真实慢点主要落在三段串行 LLM 调用，上游 `502` 会放大耗时。
  - 下一步：量化三段耗时占比，再决定按文章并发、续租、心跳拆分或失败重试策略。
  - 进行中：最新真实 compile 基线显示主要慢点为 `compile_new_articles=51.462s`、`generate_synthesis_artifacts=60.945s`，`review_articles=0.040s`；下一步先检查两段实现与配置，优先做通用耗时观测/预算治理，不做样本文档特判。
  - 已完成：已确认 `compile_new_articles` 为按文章 Writer 调用，`review_articles / fix_review_issues` 已有逐项进度刷新；`generate_synthesis_artifacts` 已是 4 类产物并行生成，慢点更可能来自单次 LLM 调用/重试预算。
  - 已完成：已补通用耗时观测日志，Writer / Reviewer / Fixer 每篇记录 `scopeId / conceptId|articleKey / current / total / durationMs / route / result`，synthesis 每个 artifact 记录 `scopeId / artifactType / durationMs / fallbackUsed / conceptSummaryChars`，失败仅记录摘要并走原 fallback。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=SynthesisArtifactsServiceTests,CompileJobLeaseManagerIntegrationTests,CompileJobLeaseManagerResilienceTests test` 通过。

- [x] 补长节点心跳刷新或分段续租
  - 状态：已完成
  - 目标：避免长编译再次触发 `COMPILE_STALE_TIMEOUT`。
  - 当前：`CompileJobLeaseManager` 已提供运行中作业后台心跳续租、步骤进度刷新续租与过期作业回收；默认 `heartbeatIntervalSeconds=15`、`leaseDurationSeconds=300`，长 Writer/Reviewer/Fixer 节点也会在每篇处理前后刷新进度。
  - 验收依据：`CompileJobLeaseManagerIntegrationTests` 覆盖后台心跳刷新、步骤进度续租、过期回收与竞争 worker 场景；`CompileJobLeaseManagerResilienceTests` 覆盖单次心跳异常后调度继续存活。

- [x] 做 PDF 抽取完整度专项对比
  - 状态：已完成
  - 范围：优先对比 `deep_evals` PDF 抽取文本与 gold Markdown。
  - 验收：不能只以问答命中代表 PDF 抽取准确率，需给出抽取缺口与改进项。
  - 进行中：仓库内未找到 `deep_evals` 与成对 gold Markdown；已按用户指定改用 `/Users/sxie/Downloads/美团充电宝订单&账单.pdf` 做真实 PDF 抽取专项验证，同时保留合成 gold PDF 回归覆盖页码、正文和表格证据。
  - 已完成：新增合成 gold PDF 回归，覆盖多页正文、页标记、PDFBox 文本抽取、坐标表格补充 `table_row` 与 `pageCount` 元数据。
  - 已完成：已用真实 PDF `/Users/sxie/Downloads/美团充电宝订单&账单.pdf` 生成专项报告 `.codex/run/pdf-extraction-audit-20260506/meituan-powerbank-report.md` 与完整抽取文本 `.codex/run/pdf-extraction-audit-20260506/meituan-powerbank-extracted.txt`；结果为 `7` 页、`6500` 字、`274` 个非空行、`55` 行 `table_row`，覆盖订单、账单、金额、支付、退款、时间、券ID、`orderId / refundFee / consumeFee / receivableFee / returnTime / lendTime` 等关键文本。
  - 缺口：复杂表格仍存在跨行/跨列拼接错位，第 6 页券月汇总账单长说明被拆成多个块；本地缺少 Poppler 渲染命令，未做版面级视觉对齐评分；没有 gold Markdown，因此不声明严格准确率。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=PdfTextExtractorTests,PdfPositionedTextTableFormatterTests,DocumentParseRouterIntegrationTests#shouldPersistFixedIngestionAccuracySampleSet test` 通过。

### A3. Query 多通道并发召回统一内核

- [x] 新增统一召回抽象
  - 状态：已完成
  - 交付：`RetrievalChannel`、`RetrievalDispatchPlan`、`RetrievalDispatchResult`、`RetrievalExecutionContext`。
  - 验收：先支持顺序执行，不引入新并发。
  - 进行中：开始落最小顺序 dispatcher 抽象；本步只统一通道接口、执行计划、上下文和结果容器，不改现有通道权重、RRF 公式、topK 或并发策略。
  - 已完成：新增 `RetrievalChannel`、`RetrievalExecutionContext`、`RetrievalDispatchPlan`、`RetrievalDispatchResult`、`RetrievalDispatcher`，当前仅按计划顺序执行，不引入并发。
  - 验收依据：`RetrievalDispatcherTests.shouldDispatchEnabledChannelsSequentiallyAndKeepDisabledChannelKeys` 覆盖启用通道顺序执行、禁用通道保留空结果 key。

- [x] 为现有通道增加 adapter
  - 状态：已完成
  - 范围：article FTS、article chunk FTS、source file、source chunk FTS、contribution、graph、refkey、article vector、chunk vector、fact card FTS、fact card vector 等现有通道。
  - 验收：通道名、分组、启用判断、结果透传、rerank 口径稳定。
  - 已完成：新增 `SupplierRetrievalChannel`，并在 `KnowledgeSearchService` 里按固定顺序适配 article FTS、article chunk FTS、refkey、source、source chunk FTS、fact card FTS、fact card vector、contribution、graph、article vector、chunk vector。
  - 验收依据：`QueryPreparationServiceTests.shouldSearchWithRewrittenQuestion` 通过，确认改写问题仍传入原底层通道，审计仍保留策略与答案形态。

- [x] `KnowledgeSearchService` 接入 dispatcher 顺序模式
  - 状态：已完成
  - 验收：`parallel_enabled=false` 时关键回归 query 的 topK 不下降，RRF 输入 channel key 和顺序不变。
  - 已完成：`KnowledgeSearchService.search` 已改为 `RetrievalDispatcher.dispatch -> RRF fuse -> review quality guardrail -> retrieval audit`，RRF 输入仍为稳定 `LinkedHashMap` channel key 顺序。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=RetrievalDispatcherTests,QueryPreparationServiceTests,RetrievalAuditServiceTests test` 通过。

- [x] Query Graph 串行路径接入 dispatcher
  - 状态：已完成
  - 验收：Graph 与 `KnowledgeSearchService` 复用同一召回内核。
  - 已完成：`retrieve_candidates_serial` 已改为 `RetrievalDispatcher.dispatch -> save dispatcher channel hits`，与 `KnowledgeSearchService` 使用同一 `RetrievalChannel / RetrievalDispatchPlan / RetrievalExecutionContext` 内核；串行路径不再重复 rerank。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=QueryGraphOrchestratorTests,QueryPreparationServiceTests,RetrievalDispatcherTests test` 通过；新增 `shouldUseDispatcherForSerialRetrievalPath` 断言 Graph 串行审计模式为 `serial` 且通道 key 顺序与统一 dispatcher 一致。

- [x] 实现 query embedding 共享
  - 状态：已完成
  - 验收：单次 query 下向量通道最多生成 1 次 query embedding。
  - 已完成：`RetrievalExecutionContext` 新增单次 dispatcher 内的 lazy query embedding 缓存；article vector、chunk vector、fact card vector 均改为只消费统一执行上下文，主链不再保留向量旧接口入口。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=RetrievalDispatcherTests,VectorSearchServiceTests,FactCardVectorSearchServiceTests,ChunkVectorSearchServiceTests,QueryPreparationServiceTests,QueryGraphOrchestratorTests test` 通过；新增 `shouldShareQueryEmbeddingAcrossVectorChannels` 断言三个向量通道只触发 1 次 embedding。

- [x] 补底层 I/O timeout 与 vector 组降级
  - 状态：已完成
  - 已完成：embedding HTTP client 已按模型配置使用 connect/read timeout；article/chunk/fact-card vector 通道的 provider 异常、维度不匹配与 repository 异常改为抛给统一 dispatcher，由 dispatcher 记录通道失败并让 lexical / graph 继续返回；并发 dispatcher 补通道级 timeout，可覆盖慢 PostgreSQL 查询。
  - 范围：embedding HTTP timeout、PostgreSQL query timeout、维度不匹配、embedding provider 异常。
  - 验收：embedding 失败不影响 lexical / graph；异常进入审计。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=RetrievalDispatcherTests,VectorSearchServiceTests,FactCardVectorSearchServiceTests,ChunkVectorSearchServiceTests,QueryPreparationServiceTests,QueryGraphOrchestratorTests,RetrievalAuditServiceTests test` 通过；新增 vector 异常隔离、共享 embedding、channel run summary 持久化回归。

- [x] 实现受控并发与失败隔离
  - 状态：已完成
  - 已完成：`RetrievalDispatchPlan` 支持 `parallelEnabled / maxConcurrency / maxConcurrencyPerGroup / channelTimeoutMillis / totalDeadlineMillis`；dispatcher 使用专用 daemon executor、通道分组 semaphore、通道级 timeout、总 deadline 与异常捕获，超时记录为 `TIMEOUT`。
  - 交付：专用执行器、通道分组、通道级 timeout、总 deadline、异常捕获、超时标记。
  - 验收：任一通道失败或超时不导致整体 Query 失败。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=RetrievalDispatcherTests test` 通过；新增慢通道超时不影响其他通道、同组并发上限生效回归。

- [x] 用 dispatcher 替换 Query Graph 多通道 `MultiCommand`
  - 状态：已完成
  - 已完成：Query Graph 已删除多通道 `MultiCommand` 入口，`dispatch_retrieval` 单节点统一调用 dispatcher；Graph 仍使用 Spring AI Alibaba Graph 做流程编排，但检索内核、并发与失败隔离只在 dispatcher 中实现。
  - 目标：避免 Graph 与 dispatcher 双重并发。
  - 验收依据：`rg -n "AsyncMultiCommandAction|MultiCommand|retrieve_candidates_parallel|retrieve_article_vector|retrieve_chunk_vector|retrieve_fact_card_vector" src/main/java --glob '!target/**'` 未命中生产实现；组合回归 `RetrievalDispatcherTests,VectorSearchServiceTests,FactCardVectorSearchServiceTests,ChunkVectorSearchServiceTests,QueryPreparationServiceTests,QueryGraphOrchestratorTests,RetrievalAuditServiceTests,QueryFacadeServiceVectorTests,QueryGraphStateMapperTests,WorkingSetStoreRoundTripTests,RedisQueryWorkingSetStoreTests,RetrievalAuditQueryServiceTests,AdminQueryRetrievalAuditControllerTests,ConfiguredVectorEmbeddingServiceTest,EmbeddingRouteResolverTest` 通过。

- [x] 新增 channel run 审计
  - 状态：已完成
  - 已完成：当前切片先把通道运行摘要写入 `query_retrieval_runs.channel_run_summary_json`，包含 channel、status、durationMillis、hitCount、skippedReason、errorSummary；本期不再新增独立明细表，避免为同一审计信息重复建模。
  - 字段：run id、channel、status、duration、hit count、timeout、error summary、skipped reason、zero-hit 标记。
  - 验收依据：`RetrievalAuditServiceTests.shouldPersistChannelRunSummary`、`RetrievalAuditQueryServiceTests`、`AdminQueryRetrievalAuditControllerTests` 通过，后台审计接口可读取 run summary JSON。

- [x] 管理后台展示 run / channel run / hit 三级明细
  - 状态：已完成
  - 已完成：后台审计接口已结构化返回 `channelRuns[]`，管理侧 ask 页面已展示 run 摘要、channel run 卡片与 hit 明细表，可区分 skipped、zero-hit、failed、timeout，并暴露耗时、命中数与错误摘要。
  - 验收依据：`node --check src/main/resources/static/admin/ask.js` 通过；`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AdminQueryRetrievalAuditControllerTests,RetrievalAuditQueryServiceTests,RetrievalAuditServiceTests,AskJsRuntimeTests test` 通过；`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -DskipTests compile` 通过。

- [x] 基于审计调整 intent / answer shape 通道组合
  - 状态：已完成
  - 已完成：基于最近审计中结构化题 fact/source 主证据占比、代码结构题 graph/source 贡献和宽泛概念题 vector/article chunk 贡献，调整通用 `QueryIntentClassifier + RetrievalStrategyResolver` 映射；带结构符号的精确标识题统一归入配置型查值策略，结构化答案形态进一步提升 fact card / source chunk 并压低背景 article/vector，宽泛概念题提升 article chunk 与向量通道。
  - 已完成：`channel_run_summary_json` 已并入 `src/main/resources/db/schema.sql`；当前不再使用 Flyway 增量迁移，DDL 由手动脚本初始化唯一 `lattice` schema。
  - 验收：结构化事实题主证据占比提升，代码结构题 graph/source 贡献提升，宽泛概念题 vector/article chunk 贡献提升。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=QueryPreparationServiceTests,QueryHitIntentRerankerTests,AnswerShapeClassifierTests test` 通过。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=RetrievalDispatcherTests,QueryPreparationServiceTests,QueryHitIntentRerankerTests,WeightedRrfFusionTest,StructuredRetrievalTopKQualityServiceTests,QueryGraphOrchestratorTests,AdminQueryRetrievalAuditControllerTests test` 通过；`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -DskipTests compile` 通过。
  - 验收依据：防特判扫描 `rg -n "scenario_id|case_num|step_index|scenarios\\.xlsx|100912|100714|100716|场景用例" src/main/java scripts --glob '!target/**'` 仅命中守门脚本 `BLOCKED_PATTERN`，未命中生产实现。

### A4. 全面超越证据收口

- [x] 建立真实问题集与回放集
  - 状态：已完成
  - 已完成：`docs/test/query-regression-suite.json` 明确真实问题集覆盖维度与标准回放产物，保留 10 个真实链路 case，覆盖运行态、结构化表格、精确 path、架构边界、配置、Deep Research 与 no-hit。
  - 已完成：`scripts/run-query-regression.mjs` 每次回放新增 `query_replay_manifest.json`，记录 suite 路径与 sha256、baseUrl、产物路径、git commit/branch/dirty、case 分布、逐题结果与 gate 指标，形成可追溯回放证据。
  - 目标：从共享 benchmark 胜出升级为真实场景稳定领先证据。
  - 验收依据：runner smoke 已用临时 suite 生成 `.codex/tmp/query-regression-manifest-smoke/query_replay_manifest.json`；`node --check scripts/run-query-regression.mjs` 通过；`node -e` 校验 suite JSON 中 `coverageDimensions` 与 `query_replay_manifest.json` 产物声明通过。

- [x] 补 retrieval recall 与最终答案准确性指标
  - 状态：已完成
  - 已完成：真实回放 runner 的 `query_summary.tsv / query_metrics.json / query_replay_manifest.json` 已新增 `retrieval_expected_count / retrieval_recall_at_5 / retrieval_recall_at_10 / retrieval_mrr / unsupported_claim_rate / citation_precision / citation_evaluable / citation_verified_count / citation_demoted_count`。
  - 已完成：`CitationCheckSummary` 对外补充 `claimCount / unsupportedClaimCount`，runner 可复算 `unsupportedClaimRate`；suite 为 path / MQ / config / Deep 题补 `retrievalTargets`，结构化题继续复用 `requiredSourceTerms` 作为目标来源。
  - 指标：`recall@5`、`recall@10`、`MRR`、`unsupportedClaimRate`、citation precision、citation coverage。
  - 验收依据：runner smoke 生成 `.codex/tmp/query-regression-metrics-smoke/query_summary.tsv`、`query_metrics.json`、`query_replay_manifest.json`，新指标字段均落盘；`node --check scripts/run-query-regression.mjs` 通过；suite JSON 校验通过。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=CitationCheckServiceTests,QueryResponseCitationAssemblerTests,QueryControllerTests test` 通过；`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -DskipTests compile` 通过。

- [x] 真实验证 Query / Deep Research / Compile 重启恢复能力
  - 状态：已完成
  - 已完成：`AstCitationDeepResearchBenchmarkRunner` 新增窄入口 `shouldGenerateResumeRecoveryReport`，只运行 Query / Deep Research / Compile checkpoint + resume smoke，不再强制跑完整 TS shadow benchmark。
  - 已完成：恢复报告已写入 `target/benchmark-reports/ast-citation-deepresearch/resume-recovery-report.json`，三条主链 `queryResumeSuccessRate / deepResearchResumeSuccessRate / compileResumeSuccessRate` 均为 `1.0`，`checkpointRecoveryReady=true`，`endToEndRecoveryReady=true`。
  - 验收：三条主链在重启场景下有可恢复证据，不能只停留在 working set 单测。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AstCitationDeepResearchBenchmarkRunner#shouldGenerateResumeRecoveryReport test` 通过；`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -DskipTests compile` 通过。

- [x] 对接 benchmark gap report 与版本对比
  - 状态：已完成
  - 已完成：完整 `AstCitationDeepResearchBenchmarkRunner#shouldGenerateGapReport` 已跑通 Java shadow、TS shadow、真实问题回放、恢复能力基准与 gap report，产物位于 `target/benchmark-reports/ast-citation-deepresearch/`。
  - 当前结论：`gap-report.md` 显示共享 benchmark 是否领先原版=是，是否已有足够证据宣称“全面超越原版”=是；`blockingGaps=[]`；证据矩阵中 `sharedBenchmark / graphStructuredFacts / retrievalRecallAt5 / retrievalRecallAt10 / mrr / realQuestionReplay / resumeRecovery / defaultConfigBenefit` 均为 `LEADING`，`retrievalAuditDashboard=AVAILABLE`。
  - 当前指标：unsupportedClaimRate Java `0.077` vs TS `0.200`；citationPrecision Java `0.889` vs TS `0.750`；citationCoverage Java `0.923` vs TS `0.800`；multiHopCompleteness Java `1.000` vs TS `0.889`；graphFactAcceptedRate Java `1.000` vs TS `0.000`；retrievalRecall@5/@10/MRR 均为 `1.000`。
  - 验收：能说明哪些问题类型已经领先，哪些问题类型仍未超过原版。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AstCitationDeepResearchBenchmarkRunner#shouldGenerateGapReport test` 通过；`node --check scripts/run-query-regression.mjs` 通过；gap report JSON 校验通过；`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -DskipTests compile` 通过。
  - 验收依据：防特判扫描 `rg -n "scenario_id|case_num|step_index|scenarios\\.xlsx|100912|100714|100716|场景用例" src/main/java scripts --glob '!target/**'` 仅命中守门脚本 `BLOCKED_PATTERN`，未命中生产实现。

---

## 3. 主线 B：复杂文档结构化证据驱动问答

### B0. 通用结构化资料查询层

- [x] 完成 B0 MVP 切片
  - 状态：已完成
  - 本期范围：XLSX / CSV 行列入库、字段等值过滤、字段投影、count / group by、行/单元格引用、10-20 题回归。
  - 后置范围：JSON/YAML、复杂跨 sheet 关联、高级排序语义、后台 UI 展示。
  - 验收依据：P0-1/P0-2/P0-3/P0-4 均已完成；本期 B0 MVP 通过 21 个结构化相关测试。

- [x] 建立通用表格结构化入库模型
  - 状态：已完成
  - 范围：XLSX、CSV、Markdown 表格，后续可扩展 JSON/YAML。
  - 交付：按 `source_path / table_name / sheet_name / row_number / column_name / cell_value / normalized_value / row_text` 保存行列事实。
  - 验收：同一资料可同时按行、列、单元格、整行全文检索；不依赖具体文件名、业务名、列名或样例值。
  - 本期切片：先覆盖 XLSX / CSV；Markdown 表格作为后置增强。
  - 验收依据：XLSX / CSV 切片已通过 `StructuredTableJdbcRepositoryTests,DocumentParseRouterIntegrationTests`；Markdown 表格仍为后置增强，不纳入本期 P0。

- [x] 增加表格 schema profiling
  - 状态：已完成
  - 能力：识别高唯一度列、枚举列、数值列、日期列、长文本列、疑似主键列、可聚合列。
  - 验收：任意表格导入后生成字段画像；`scenario_id / case_num / step_index` 仅作为普通列被画像识别，不进入硬编码分支。
  - 已完成：新增通用 `StructuredTableSchemaProfiler`，按非空率、唯一度、值类型占比、文本长度和 distinct 数生成 `schemaProfile`，导入结构化表时写入表级 `metadata_json`。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=StructuredTableJdbcRepositoryTests test`，3 个测试通过；防特判扫描未命中主链实现和本次通用测试样本。

- [x] 增加结构化 Query Plan 路由
  - 状态：已完成
  - 触发：`字段=值`、某一行、分别是什么、多少条、各多少、按字段统计、对比两行、第几步、最大/最小/排序/过滤。
  - 交付：`StructuredQueryPlan`、字段匹配、表选择、过滤、投影、聚合、排序、limit、回退原因。
  - 验收：能把精确字段题与聚合题路由到确定性查询；无法形成计划时才回退普通 RAG。
  - 验收依据：字段等值过滤、字段投影、count、group by、两行对比已通过 planner 与接口回归；最大/最小/排序作为后置增强。

- [x] 实现结构化查询执行器
  - 状态：已完成
  - 能力：行定位、字段投影、单元格返回、枚举统计、数值聚合、两组行对比、步骤/序号类过滤。
  - 验收：LLM 不负责数数或猜字段值，只负责把查询结果组织成自然语言。
  - 验收依据：字段投影、count、group by、两行对比已走 `RULE_BASED / STRUCTURED_QUERY`；数值聚合、排序作为后置增强。

- [x] 增加结构化证据对象与行/单元格引用
  - 状态：已完成
  - 交付：答案引用能指向 `source_path + sheet/table + row_number + column_name`，并保留原始单元格值。
  - 验收：回答字段值必须能回指到对应单元格；聚合结果必须能回指到参与计算的表与过滤条件。
  - 验收依据：`structuredEvidence.rows[].cells[]` 已返回列名、列序号、原始值、归一化值与证据角色；接口回归已断言 sourcePath、rowNumber、列名和 role。

- [x] 升级引用校验到字段级与聚合级
  - 状态：已完成
  - 规则：问 `字段=值` 时，引用行必须满足过滤条件；问字段值时，答案必须来自目标字段；问统计时，答案必须可由结构化查询复算。
  - 验收：目标行不匹配、字段值不匹配、聚合不可复算时不能 `PASSED`。
  - 验收依据：结构化短路内新增过滤行、投影字段、分组聚合最小一致性校验；不一致时不返回 `STRUCTURED_QUERY` 通过答案。

- [x] 控制结构化资料问答上下文大小
  - 状态：已完成
  - 目标：结构化查询只把目标行、目标字段和必要邻接上下文交给 LLM。
  - 验收：避免把整张表或超长结构化文章塞入 `query-answer-structured`；长表问题不再触发 64k 截断。
  - 验收依据：本期结构化题直接由 `RULE_BASED / STRUCTURED_QUERY` 返回目标行、字段和聚合证据，不进入 LLM 上下文。

- [x] 建立结构化资料固定回归集
  - 状态：已完成
  - 范围：精确字段抽取、行定位、枚举统计、数值统计、两行对比、跨 sheet 关联、步骤明细、未知字段/无命中。
  - 验收：每题记录结构化计划、查询结果、最终答案、行/单元格引用、人工判定；回归样本可包含 `scenarios.xlsx`，但实现不得依赖该文件。
  - 验收依据：新增 `StructuredTableQueryRegressionTests` 固定 10 题；跨 sheet 关联、数值高级统计作为后置增强，不纳入本期 P0。

### B1. 结构化题回归

- [x] 结构化题分类回归
  - 状态：已完成
  - 范围：枚举、对照、顺序、状态、规则、通用题。
  - 验收：各类型分类准确率均 `> 70%`，且样例不依赖业务专名。
  - 已完成：新增六类通用题型分类回归集，覆盖枚举、对照、顺序、状态、规则、通用题；同步把答案形态分类与结构化事实意图分类中的偏样例状态信号收敛为通用 `结论 / 数据 / 指标 / 趋势 / 调整 / 修正 / 降级` 信号。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dtest=AnswerShapeClassifierTests,QueryPreparationServiceTests,QueryHitIntentRerankerTests,WeightedRrfFusionTest test`，27 个测试通过。
  - 组合回归：`mvn -s .codex/maven-settings.xml -Dtest=StructuredQueryPlannerTests,StructuredTableJdbcRepositoryTests,DocumentParseRouterIntegrationTests,QueryControllerTests,StructuredTableQueryRegressionTests,AnswerShapeClassifierTests,QueryPreparationServiceTests,QueryHitIntentRerankerTests,WeightedRrfFusionTest test`，48 个测试通过。

- [x] 当前 15 题、结构化题集、非卡券复杂文档全部完成回归
  - 状态：已完成
  - 当前：当前 15 题固定回归已达标；结构化表格 10 题回归与非卡券复杂文档最小切片已通过自动化测试。
  - 已完成：新增统一入口 `scripts/run-b1-structured-regression.sh`，集中执行 B1 自动化回归、可选 15 题 TSV 门槛校验、防特判扫描；脚本不内置业务题目，15 题结果通过 `QUERY_SUMMARY_TSV` 传入。
  - 验收依据：`scripts/run-b1-structured-regression.sh` 通过，覆盖 49 个自动化测试与防特判扫描。
  - 统一门槛验收依据：`QUERY_SUMMARY_TSV=.codex/run/card-regression-query-summary-20260505.tsv scripts/run-b1-structured-regression.sh` 通过，15 行查询摘要全部满足 `http_status=200 / review_status=PASSED / answer_outcome=SUCCESS`，同时 49 个自动化测试与防特判扫描通过。

- [x] 非卡券复杂文档回归
  - 状态：已完成
  - 样本：至少 2 份复杂文档，覆盖表格、列表、状态、顺序、对照、规则。
  - 候选：AI Agent、`deep_evals`、公司业务方案等。
  - 验收：能生成同类 fact card，query 无明显回退。
  - 已完成：新增 `NonCouponComplexDocumentRegressionTests`，用两份通用复杂文档样本覆盖表格、列表、状态、顺序、对照、规则，验证 `FACT_ENUM / FACT_COMPARE / FACT_SEQUENCE / FACT_STATUS / FACT_POLICY` 生成、source chunk 回指、FTS 召回、完整答案覆盖与漏答识别。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dtest=NonCouponComplexDocumentRegressionTests test`，1 个测试通过。
  - 组合回归：`mvn -s .codex/maven-settings.xml -Dtest=StructuredQueryPlannerTests,StructuredTableJdbcRepositoryTests,DocumentParseRouterIntegrationTests,QueryControllerTests,StructuredTableQueryRegressionTests,AnswerShapeClassifierTests,QueryPreparationServiceTests,QueryHitIntentRerankerTests,WeightedRrfFusionTest,NonCouponComplexDocumentRegressionTests test`，49 个测试通过。

### B2. 证据层与检索层指标

- [x] 证据层指标验收
  - 状态：已完成
  - 验收：fact card source 回指通过率 `> 90%`，状态可解释率 `100%`，低置信、冲突、缺边界卡片均有可解释状态。
  - 已完成：新增 `FactCardEvidenceQualityService` / `FactCardEvidenceQualityReport` / `FactCardEvidenceQualitySample`，按样本统计 source 回指通过率、状态可解释率、问题状态原因覆盖率。
  - 验证：`mvn -s .codex/maven-settings.xml -Dtest=FactCardEvidenceQualityServiceTests test` 通过，覆盖正常卡片、低置信、冲突、缺边界与无原因问题样本。
  - 统一入口：`QUERY_SUMMARY_TSV=.codex/run/card-regression-query-summary-20260505.tsv scripts/run-b1-structured-regression.sh` 通过，覆盖 64 个自动化测试、15 行 TSV 门槛与防特判扫描。

- [x] 检索层指标验收
  - 状态：已完成
  - 验收：结构化题目标证据进入 topK 比例 `> 80%`。
  - 已完成：新增 `StructuredRetrievalTopKQualityService` / `StructuredRetrievalTopKReport` / `StructuredRetrievalTopKSample` / `StructuredRetrievalTopKTarget`，按通用 `evidenceType + articleKey/conceptId` 统计目标证据进入 topK 比例和样本全覆盖比例。
  - 验证：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=StructuredRetrievalTopKQualityServiceTests test` 通过，覆盖结构化主证据保护、缺失目标失败、`articleKey` / `conceptId` 双身份匹配。
  - 统一入口：`QUERY_SUMMARY_TSV=.codex/run/card-regression-query-summary-20260505.tsv scripts/run-b1-structured-regression.sh` 通过，覆盖 64 个自动化测试、15 行 TSV 门槛与防特判扫描。

- [x] 回答覆盖校验进入真实回归
  - 状态：已完成
  - 验收：枚举漏项、对照单侧、顺序乱序、状态混淆在真实题集中可被识别并留痕。
  - 已完成：`AnswerCoverageCheckServiceTests` 已覆盖完整覆盖、枚举漏项、对照单侧、顺序乱序、状态混淆、状态聚焦、规则范围缺失、通用题不适用、无结构化证据缺失等 9 类样本，并纳入统一回归入口。
  - 验证：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AnswerCoverageCheckServiceTests test` 通过，9 个测试通过。
  - 统一入口：`QUERY_SUMMARY_TSV=.codex/run/card-regression-query-summary-20260505.tsv scripts/run-b1-structured-regression.sh` 通过，覆盖 64 个自动化测试、15 行 TSV 门槛与防特判扫描。

- [x] 结构化问答防特判扫描常态化
  - 状态：已完成
  - 验收：每轮结构化回归后执行主链业务专名扫描，并把结果写回本文。
  - 本轮扫描：已执行 `rg -n "scenario_id|case_num|step_index|scenarios\.xlsx|100912|100978|100979|100914" src/main/java src/test/java/com/xbk/lattice/api/query src/test/java/com/xbk/lattice/query/structured src/test/java/com/xbk/lattice/infra/persistence/StructuredTableJdbcRepositoryTests.java docs/plans/2026-05-05-当前剩余工作总清单.md`。
  - 结果：命中仅在本文红线与验收口径说明中，主链实现、结构化查询实现与本期通用回归样本未发现特定文档、特定列名、特定样例值硬编码。
  - 追加扫描：已执行 `rg -n "scenario_id|case_num|step_index|scenarios\.xlsx|100912|100978|100979|100914|流量结论|featureToggleList" src/main/java/com/xbk/lattice/query/service/AnswerShapeClassifier.java src/main/java/com/xbk/lattice/query/service/QueryIntentClassifier.java src/test/java/com/xbk/lattice/query/service/AnswerShapeClassifierTests.java src/test/java/com/xbk/lattice/query/service/QueryHitIntentRerankerTests.java src/main/java/com/xbk/lattice/query/structured src/test/java/com/xbk/lattice/query/structured src/test/java/com/xbk/lattice/api/query/StructuredTableQueryRegressionTests.java docs/plans/2026-05-05-当前剩余工作总清单.md`。
  - 追加结果：命中仅在本文红线、验收口径和扫描命令说明中；结构化查询、答案形态分类、意图分类与本期通用回归样本未发现特定文档、特定列名、特定样例值或偏样例状态词硬编码。
  - 本轮追加扫描：已执行 `rg -n "scenario_id|case_num|step_index|scenarios\.xlsx|100912|100978|100979|100914|流量结论|featureToggleList" src/main/java/com/xbk/lattice/query/service/AnswerShapeClassifier.java src/main/java/com/xbk/lattice/query/service/QueryIntentClassifier.java src/main/java/com/xbk/lattice/query/structured src/test/java/com/xbk/lattice/query/service/AnswerShapeClassifierTests.java src/test/java/com/xbk/lattice/query/service/QueryHitIntentRerankerTests.java src/test/java/com/xbk/lattice/query/service/NonCouponComplexDocumentRegressionTests.java src/test/java/com/xbk/lattice/query/structured src/test/java/com/xbk/lattice/api/query/StructuredTableQueryRegressionTests.java docs/plans/2026-05-05-当前剩余工作总清单.md`。
  - 本轮追加结果：命中仅在本文红线、验收口径和扫描命令说明中；主链实现、结构化查询实现、题型分类、意图分类、结构化表格回归与非卡券复杂文档回归未发现特定文档、特定列名、特定样例值或偏样例状态词硬编码。
  - 统一入口结果：`scripts/run-b1-structured-regression.sh` 已把同类扫描纳入 B1/B2 统一门槛；本轮无命中。
  - 后续要求：每轮结构化回归继续执行同类扫描；若新增主链、结构化查询、分类、覆盖校验或通用样本文件，需要同步加入 `SCAN_TARGETS`。

### B3. 最终结论

- [x] 文档更新最终结论和遗留风险
  - 状态：已完成
  - 结论：本期 P0 结构化问答主链已收口到“通用结构化查询 + 通用证据质量门禁 + 通用检索 topK 门禁 + 通用覆盖校验门禁”。本轮没有为 `scenario_id`、`case_num`、`step_index`、`scenarios.xlsx` 或具体样例值增加特判。
  - 本轮门禁：`QUERY_SUMMARY_TSV=.codex/run/card-regression-query-summary-20260505.tsv scripts/run-b1-structured-regression.sh` 通过，包含结构化查询、非卡券复杂文档、证据层指标、检索层 topK 指标、回答覆盖校验、15 行真实 query TSV 与防特判扫描。
  - 遗留风险：真实线上准确率仍取决于后续资料编译质量、真实 query 样本覆盖面、检索 audit 样本沉淀和 C 线治理闭环；C 线不纳入本轮 P0 完成率。
  - 下一步：进入 C1 内容治理最小闭环前，先用当前 18082 环境追加跑一轮人工刁钻问题，若失败项属于资料抽取或结构化入库，再回到 B0/B2 门禁补通用样本。

---

## 4. 主线 C：内容治理、风险抽检与结果驱动复核

### C1. 内容治理最小闭环

- [x] 扩展文章列表查询条件
  - 状态：已完成
  - 位置：`AdminArticleController.list`
  - 参数：`reviewStatus`
  - 验收：保留现有 `query / lifecycle / sourceId` 能力，组合查询不互相覆盖。
  - 已完成：`AdminArticleController.list` 新增 `reviewStatus` 入参，并透传到 `AdminArticleQueryService.list(query, lifecycle, sourceId, reviewStatus)`。
  - 验证：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AdminArticleQueryServiceTests test` 通过，2 个测试通过。

- [x] 扩展文章查询服务
  - 状态：已完成
  - 位置：`AdminArticleQueryService`
  - 验收：支持按 `review_status` 过滤，空 `reviewStatus` 时行为与当前一致。
  - 已完成：新增 `reviewStatus` 规范化过滤，空值忽略；组合筛选覆盖 `query / lifecycle / sourceId / reviewStatus`。
  - 验证：`AdminArticleQueryServiceTests` 覆盖组合过滤与空复核状态不改变既有行为。

- [x] 新增人工复核服务
  - 状态：已完成
  - 建议类：`ArticleManualReviewService`
  - 职责：校验文章存在，支持 `articleKey / conceptId + sourceId` 定位，校验状态流转，更新 `articles.review_status`，同步正文 frontmatter，保存快照，写入审计。
  - 已完成：新增通用人工复核服务，支持确认通过、提交修正、审计查询、期望状态冲突校验和 source-aware 文章定位。
  - 验证：`ArticleManualReviewServiceTests` 覆盖确认通过、提交修正、复核历史、sourceId 消歧入口、并发状态校验、审计、快照和 frontmatter 同步。

- [x] 新增复核审计持久化
  - 状态：已完成
  - 表：`article_review_audits`
  - 字段：`id`、`source_id`、`article_key`、`concept_id`、`action`、`previous_review_status`、`next_review_status`、`comment`、`reviewed_by`、`reviewed_at`、`metadata_json`。
  - 已完成：在单一 `src/main/resources/db/schema.sql` 中补充人工复核审计表、索引、字段注释，并新增 `ArticleReviewAuditRecord` 与 `ArticleReviewAuditJdbcRepository`。
  - 验证：`ArticleReviewAuditJdbcRepositoryTests` 覆盖手动 DDL 建表、保存后返回自增主键、按 `articleKey` 查询、无 `articleKey` 时按 `conceptId + sourceId` 查询。

- [x] 新增人工复核接口
  - 状态：已完成
  - 接口：`POST /api/v1/admin/articles/{articleId}/review/approve`
  - 接口：`POST /api/v1/admin/articles/{articleId}/review/request-changes`
  - 接口：`GET /api/v1/admin/articles/{articleId}/review/audits`
  - 请求字段：`sourceId`、`reviewedBy`、`comment`、`expectedReviewStatus`、`correctionSummary`。
  - 响应字段：`articleKey`、`conceptId`、`previousReviewStatus`、`reviewStatus`、`reviewedBy`、`reviewedAt`、`auditId`。
  - 已完成：`AdminArticleController` 新增确认通过、提交修正、复核审计接口，并补齐 Admin 请求/响应 DTO。
  - 验证：`AdminGovernanceApiIntegrationTests.shouldExposeArticleManualReviewApproveApis` 与 `shouldExposeArticleManualReviewRequestChangesApi` 覆盖 HTTP 接口、复核历史与状态联动。

- [x] 人工确认通过时保存快照
  - 状态：已完成
  - 快照原因：`manual_review_approve`
  - 验收：确认通过后 `article_snapshots.review_status=passed` 有新增记录。
  - 验证：`ArticleManualReviewServiceTests.shouldApproveArticleAndPersistSnapshotAndAudit` 断言新增快照 `snapshotReason=manual_review_approve` 且 `reviewStatus=passed`。

- [x] 确认通过时同步正文 frontmatter
  - 状态：已完成
  - 验收：文章正文 frontmatter 中 `review_status` 同步为 `passed`。
  - 验证：`ArticleManualReviewServiceTests.shouldApproveArticleAndPersistSnapshotAndAudit` 断言文章正文包含 `review_status: passed`。

- [x] 提交修正时复用现有纠错链路
  - 状态：已完成
  - 期望：复用 `ArticleCorrectionService.correct`，修正后状态保持 `needs_review`，审计记录写 `request_changes`。
  - 验证：`ArticleManualReviewServiceTests.shouldRequestChangesThroughCorrectionServiceAndAudit` 断言调用 `ArticleCorrectionService.correct(articleId, sourceId, correctionSummary)`，文章状态保持 `needs_review`，审计动作为 `request_changes`。

- [x] 更新总览与质量统计口径
  - 状态：已完成
  - 范围：`StatusService`、`QualityMetricsService`、`/api/v1/admin/overview`、`/api/v1/admin/quality`。
  - 验收：确认通过后 `reviewPendingArticleCount` 与 `needsHumanReviewArticles` 同步下降。
  - 已完成：现有 `StatusService` 与 `QualityMetricsService` 已从 `articles.review_status` 实时汇总，人工复核通过写回文章状态后无需额外分支。
  - 验证：`ArticleManualReviewServiceTests.shouldUpdateOverviewAndQualityMetricsAfterApproval` 断言确认通过后 `reviewPendingArticleCount=0`、`needsHumanReviewArticles=0`。

- [x] 后端服务与接口测试
  - 状态：已完成
  - 建议：`ArticleManualReviewServiceTests`、`AdminArticleReviewControllerTests`。
  - 覆盖：确认通过、提交修正、复核历史、`reviewStatus` 筛选、`sourceId` 消歧、并发状态校验、审计、快照。
  - 验证：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=ArticleManualReviewServiceTests,ArticleReviewAuditJdbcRepositoryTests,AdminArticleQueryServiceTests test` 通过，10 个测试通过。
  - 验证：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AdminGovernanceApiIntegrationTests test` 通过，10 个接口集成测试通过。

### C2. 内容治理前端闭环

- [x] 在“已入库内容”工具栏增加复核状态筛选
  - 状态：已完成
  - 选项：全部复核状态、已通过、待处理、待复核、需人工复核。
  - 验收：选择“需人工复核”后只展示 `needs_human_review` 条目。
  - 已完成：工作台筛选控件已接入 `reviewStatus` 通用请求参数，不针对特定资料或列名。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=ManagementJsRuntimeTests,AdminPageControllerTests,AdminSourceControllerTests test`，8 个测试通过。

- [x] 在文章详情页增加人工复核操作区
  - 状态：已完成
  - 展示条件：`reviewStatus=needs_human_review` 或 `reviewStatus=needs_review`。
  - 控件：复核人、复核意见、确认通过、提交修正。
  - 已完成：文章详情区已新增人工复核表单、默认复核人、状态冲突前置字段和通用展示条件。
  - 验收依据：`ManagementJsRuntimeTests` 已覆盖 `needs_human_review / needs_review / passed` 三类展示条件。

- [x] 完成“确认通过”交互
  - 状态：已完成
  - 验收：成功后刷新文章详情、文章列表、状态摘要，徽标变为已通过；状态变化冲突时给出错误提示。
  - 已完成：前端已接入 `POST /review/approve`，成功后刷新摘要、列表与详情；后端接口已做 expectedReviewStatus 冲突校验。
  - 验收依据：后端 `AdminGovernanceApiIntegrationTests` 与前端 `ManagementJsRuntimeTests` 均已覆盖确认通过路径。

- [x] 完成“提交修正”交互
  - 状态：已完成
  - 验收：必须填写修正意见，成功后展示修正结果，新状态不直接变为 `passed`。
  - 已完成：前端已接入 `POST /review/request-changes`，修正意见必填；后端复用现有纠错链路并把状态收敛为 `needs_review`。
  - 验收依据：后端 `AdminGovernanceApiIntegrationTests` 与前端 `ManagementJsRuntimeTests` 均已覆盖提交修正路径。

- [x] 在详情页展示来源辅助信息
  - 状态：已完成
  - P0：保留主要参考文件和完整来源列表，支持加载 source 文件预览，大文件只展示 `contentPreview`。
  - 已完成：资料源文件响应已透出 `contentPreview`，文章来源列表可在匹配 source 文件时加载预览，只展示预览文本。
  - 验收依据：`AdminSourceControllerTests` 覆盖 `contentPreview` 响应；`ManagementJsRuntimeTests` 覆盖来源预览渲染。

- [x] 在详情页展示复核历史
  - 状态：已完成
  - 内容：动作、前后状态、复核人、时间、意见。
  - 已完成：文章详情加载时并行请求 `GET /review/audits`，渲染动作、前后状态、复核人、时间和意见。
  - 验收依据：`ManagementJsRuntimeTests` 已覆盖复核历史渲染；后端审计查询由 `ArticleReviewAuditJdbcRepositoryTests` 与 `AdminGovernanceApiIntegrationTests` 覆盖。

- [x] 状态摘要文案同步
  - 状态：已完成
  - 要求：引导到“已入库内容 -> 复核状态筛选”，避免暗示用户需要全量审核。
  - 已完成：状态摘要新增“需复核内容”卡片，帮助文案引导到“已入库内容”并使用复核状态筛选。
  - 验收依据：`ManagementJsRuntimeTests` 已覆盖摘要卡片和 help state 文案。

- [x] 前端静态行为测试
  - 状态：已完成
  - 覆盖：徽标渲染、筛选参数、详情操作区展示条件、确认通过刷新、提交修正缺少意见拦截。
  - 已完成：补充 Node harness 和页面资源断言，覆盖复核筛选 URL、操作区展示、请求载荷、复核历史、来源预览和摘要引导。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=ManagementJsRuntimeTests,AdminPageControllerTests,AdminSourceControllerTests test`，8 个测试通过。

- [x] 真实链路验收
  - 状态：已完成
  - 验收：准备一篇 `needs_human_review` 文章，在 `/admin?tab=knowledge-articles` 完成筛选、详情查看、确认通过、统计刷新、审计查询。
  - 已完成：在独立 schema `lattice_c2_review_20260505`、端口 `18082` 上准备两篇验收文章，完成 HTTP 与真实页面路径验证。
  - 验收依据：健康检查 `UP`；静态资源版本 `20260505-review-flow-1`；`reviewStatus=needs_human_review` 筛选命中 1 条；确认通过后需复核列表为 0、已通过列表为 1、overview `reviewPendingArticleCount=0`、frontmatter 同步为 `review_status: passed`、审计记录包含 `approve / needs_human_review -> passed`。
  - 浏览器验收：Playwright 打开 `/admin?tab=knowledge-articles`，完成复核状态筛选、详情查看、来源预览、确认通过、统计刷新与审计历史展示；确认通过后人工复核面板隐藏。
  - 环境说明：该记录为历史验收口径；当前不再为每轮验收创建独立 schema，统一使用手动 DDL 重建后的 `lattice` schema。

### C3. 风险维度建模

- [x] 明确 `review_status` 与风险维度职责
  - 状态：已完成
  - 规则：`review_status` 只表示内容审核状态；`risk_level / risk_reasons` 表示风险；答案反馈不强行写回文章审核状态。
  - 已完成：已把风险维度从文章审核状态中拆出，落到通用字段、响应和治理统计，不引入特定资料或特定业务词特判；18082 真实链路复验通过。

- [x] 新增风险字段或独立风险表
  - 状态：已完成
  - 字段：`risk_level=low/medium/high`、`risk_reasons`。
  - 原因枚举：`source_conflict`、`low_traceability`、`ocr_low_confidence`、`hotspot_unverified`、`user_reported`。
  - 已完成：已采用文章通用风险字段切片，支持 `risk_level / risk_reasons / is_hotspot / requires_result_verification`，并补 `risk_level / risk_reasons / hotspot verification` 索引。

- [x] 文章列表 / 详情返回风险信息
  - 状态：已完成
  - 字段：`riskLevel`、`riskReasons`、`isHotspot`、`requiresResultVerification`。
  - 已完成：管理侧列表 / 详情已透传 `riskLevel / riskReasons / isHotspot / requiresResultVerification`；18082 API 验证高风险和用户反馈筛选均命中通用样本。

- [x] 前端展示风险原因
  - 状态：已完成
  - 位置：已入库内容列表、详情面板、首页治理摘要。
  - 文案：风险提示优先说明“不稳在哪里、依据在哪里”，不要要求用户全文审核。
  - 已完成：列表、详情与首页摘要已展示风险等级、风险原因、热点和抽检状态；浏览器快照确认列表高风险徽标、详情风险提示、技术信息均正常展示。

- [x] 风险筛选与治理统计
  - 状态：已完成
  - 筛选：高风险内容、高频热点、来源冲突、用户反馈。
  - 统计：`highRiskArticleCount`、`hotspotPendingVerificationCount`、`userReportedAnswerCount`。
  - 已完成：已接入 `riskLevel / riskReason / isHotspot / requiresResultVerification` 通用筛选参数和三项治理统计，不复用 `review_status` 表达风险；overview 实测三项统计均为 1，浏览器首页摘要同步展示。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AdminArticleQueryServiceTests,StatusServiceTests,ManagementJsRuntimeTests,AdminPageControllerTests,ArticleJdbcRepositoryTests test`，13 个测试通过；`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AdminGovernanceApiIntegrationTests,ArticleManualReviewServiceTests,LatticeHttpClientTests,LatticeMcpToolsB7Test test`，35 个测试通过；18082 / `lattice_c3_risk_20260505` 真实 HTTP 与浏览器快照通过。

### C4. 答案结果反馈回流

- [x] 新增答案反馈持久化
  - 状态：已完成
  - 建议：`pending_answer_feedback`、`answer_feedback_audits`。
  - 原则：某次答案有问题，不等于整篇文章不通过。
  - 已完成：已新增结果反馈独立持久化和审计表、JDBC 记录与仓储，不写回文章审核状态。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AnswerFeedbackJdbcRepositoryTests,AdminGovernanceApiIntegrationTests test`，13 个测试通过。
  - 组合回归：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AdminGovernanceApiIntegrationTests,ArticleManualReviewServiceTests,LatticeHttpClientTests,LatticeMcpToolsB7Test,AnswerFeedbackServiceTests,StatusServiceTests,AskJsRuntimeTests,ManagementJsRuntimeTests,AdminPageControllerTests,AnswerFeedbackJdbcRepositoryTests test`，45 个测试通过。
  - 真实链路：历史记录为 18082 / 独立 schema 验收；当前反馈表已并入 `src/main/resources/db/schema.sql`，统一由手动 DDL 初始化。

- [x] 新增答案反馈接口
  - 状态：已完成
  - 接口：`POST /api/v1/admin/query-feedback`
  - 接口：`GET /api/v1/admin/query-feedback`
  - 接口：`GET /api/v1/admin/query-feedback/{id}`
  - 字段：`queryId`、`question`、`answerSummary`、`feedbackType`、`comment`、`articleKeys`、`sourcePaths`、`reportedBy`。
  - 已完成：已新增创建、列表、详情、resolve、dismiss 接口和服务层审计。
  - 验收依据：`AdminGovernanceApiIntegrationTests.shouldExposeQueryFeedbackApis` 覆盖创建、列表、详情、处理审计与 overview 待处理统计。
  - 组合回归：同 C4 组合回归，45 个测试通过。
  - 真实链路：`POST /api/v1/admin/query-feedback` 返回 `id=1 / status=PENDING`；`GET /api/v1/admin/query-feedback?status=PENDING&limit=10` 返回 1 条；`GET /api/v1/admin/query-feedback/1` 返回关联问题、答案摘要、文章、来源和 `CREATE` 审计。

- [x] 问答页增加反馈入口
  - 状态：已完成
  - 选项：答案可靠、答案有问题、来源冲突、需要人工确认。
  - 验收：反馈后进入治理队列，并关联问题、答案、引用来源、命中文章或证据节点。
  - 已完成：已在问答页新增结果反馈入口，并把最后一次问答的 `queryId / question / answer / articles / sources / searchItems` 归并为通用反馈请求。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AnswerFeedbackServiceTests,StatusServiceTests,AskJsRuntimeTests,ManagementJsRuntimeTests,AdminPageControllerTests test`，7 个测试通过。
  - 组合回归：同 C4 组合回归，45 个测试通过。
  - 真实链路：18082 `/admin/ask.html` 已加载 `ask-feedback-panel / ask-feedback-type / submit-answer-feedback` 和资源版本 `20260505-answer-feedback-1`；真实提交链路由反馈 API 与治理队列闭环验证。

- [x] 后台增加反馈处理队列
  - 状态：已完成
  - 验收：管理员能查看反馈详情、关联来源、处理状态、审计历史。
  - 已完成：已在工作台新增“结果反馈”标签页，支持列表、详情、关联文章/来源、处理历史、标记已处理和标记忽略。
  - 验收依据：`ManagementJsRuntimeTests` 覆盖结果反馈列表 URL、展示文案、处理请求；`AdminGovernanceApiIntegrationTests.shouldExposeQueryFeedbackApis` 覆盖处理审计。
  - 组合回归：同 C4 组合回归，45 个测试通过。
  - 真实链路：Playwright 打开 `/admin?tab=knowledge-feedback`，页面展示待处理反馈、详情、关联文章 `real-article-1`、来源 `docs/api.md`、`CREATE` 历史；填写处理人和说明后点击“标记已处理”，待处理列表变为 0。

- [x] 工作台首页增加结果反馈待处理摘要
  - 状态：已完成
  - 文案：突出少量高价值治理任务，不制造全量审核压力。
  - 已完成：已把 `answerFeedbackPendingCount` 纳入首页摘要和引导，入口指向“结果反馈”。
  - 验收依据：`StatusServiceTests` 覆盖统计汇总；`ManagementJsRuntimeTests` 覆盖摘要卡片和帮助入口；`AdminGovernanceApiIntegrationTests.shouldExposeQueryFeedbackApis` 覆盖真实接口统计变化。
  - 组合回归：同 C4 组合回归，45 个测试通过。
  - 真实链路：创建反馈后 overview `answerFeedbackPendingCount=1`，页面“结果反馈待处理”展示 1；标记已处理后 overview 与页面均归零。

### C5. 高频热点抽检

- [x] 建立内容命中与引用热度统计
  - 状态：已完成
  - 来源：搜索命中、Query 引用、答案反馈、人工标记。
  - 已完成：新增 `article_usage_stats` 通用统计表和 `ArticleUsageStatsJdbcRepository`，从 `query_retrieval_channel_hits`、`query_answer_citations`、`pending_answer_feedback` 与既有 `articles.is_hotspot` 汇总热度分，不按具体文档、字段名或业务样例特判。
  - 验收依据：`ArticleUsageStatsJdbcRepositoryTests.shouldRebuildStatsFromGenericSignals` 覆盖检索命中、答案引用、结果反馈和可靠反馈排除；本轮组合回归 18 个测试通过。

- [x] 生成热点待抽检队列
  - 状态：已完成
  - 验收：高频内容可标记 `isHotspot`，并进入 `hotspotPendingVerificationCount`。
  - 已完成：新增 `ArticleHotspotRefreshService` 与 `POST /api/v1/admin/articles/hotspots/refresh`，达到热度阈值的文章会标记 `is_hotspot=true`、`requires_result_verification=true`、风险原因追加 `hotspot_unverified`。
  - 验收依据：`ArticleUsageStatsJdbcRepositoryTests.shouldRefreshHotspotQueueAndMarkArticles` 与 `AdminGovernanceApiIntegrationTests.shouldExposeArticleHotspotRefreshApi` 覆盖队列生成、文章筛选和 overview `hotspotPendingVerificationCount=1`。

- [x] 已入库内容页支持热点筛选
  - 状态：已完成
  - 选项：高频热点、高风险内容、来源冲突、用户反馈。
  - 已完成：沿用现有通用风险筛选参数 `isHotspot / requiresResultVerification / riskLevel / riskReason`，并在“已入库内容”页新增刷新热点按钮，刷新后自动切到“待结果抽检”队列。
  - 验收依据：`ManagementJsRuntimeTests` 覆盖热点刷新状态与筛选引导；`AdminPageControllerTests` 覆盖页面按钮与资源版本；组合回归 18 个测试通过。

- [x] 首页展示热点抽检入口
  - 状态：已完成
  - 文案：例如“存在 N 条高频热点内容，建议确认”。
  - 已完成：工作台首页摘要继续展示“热点待抽检”，并在没有人工复核和结果反馈积压时优先引导到“已入库内容”的待结果抽检/高频热点筛选。
  - 验收依据：`ManagementJsRuntimeTests` 覆盖 `hotspotPendingVerificationCount` 下的首页帮助入口；组合回归 18 个测试通过。

### C6. 治理增强 backlog（非本期）

以下事项不纳入本期 P0 完成口径，只在 P0 / P1 主线稳定后择机推进；不使用普通 checkbox，避免影响本轮“全部完成”判断。

- Backlog：支持重新标记为需人工复核
  - 状态：未开始

- Backlog：支持批量确认通过
  - 状态：未开始
  - 限制：必须二次确认并填写批量说明。

- Backlog：来源预览做成真正的复核视图
  - 状态：未开始
  - 能力：左侧文章正文、右侧来源片段、PDF 页码、表格块、chunk 编号。

- Backlog：复核任务队列
  - 状态：未开始
  - 说明：P0 先在文章详情页闭环，稳定后再抽独立队列。

- Backlog：复核指标历史
  - 状态：未开始
  - 指标：待复核数量、复核通过数量、修正数量、平均处理时间、按资料源统计待复核比例。

---

## 5. 主线 D：管理后台处理任务可见性收口

- [x] 资料源详情联动处理历史
  - 状态：已完成
  - 目标：把资料源详情中的“同步历史”升级为“处理历史”，纳入关联 compile 处理。
  - 已完成：新增资料源维度处理历史接口，资料源详情统一展示 source sync 与关联 standalone compile；前端文案从“同步历史”收口为“处理历史”。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AdminUploadControllerTests,ManagementJsRuntimeTests,AdminPageControllerTests test`，12 个测试通过。

- [x] 当前处理任务真实回归
  - 状态：已完成
  - 场景：上传文件触发同步、Git 或目录资料源同步、direct compile `docs/`、人为制造 `STALLED`。
  - 验收：首页不能在后台活跃处理时显示空闲；direct compile 必须可见；`RUNNING -> STALLED` 必须显式可见；关联同步与 compile 不能重复展示。
  - 已完成：自动化回归覆盖上传同步、目录资料源同步、direct compile、STALLED 与关联 compile 去重；HTTP 真链路在 18082 独立 schema 上验证 upload/source sync/direct compile 均可见，direct compile 人工置旧心跳后显示 `STALLED`。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AdminProcessingTaskControllerTests test`，4 个测试通过；18082 临时实例 schema=`lattice_d2_processing_real_20260506`，产物目录 `/tmp/lattice-d2-processing-real-20260506`，最终 `processing-tasks-after-server-and-stalled.json` 返回 `SOURCE_SYNC/UPLOAD`、`SOURCE_SYNC/SERVER_DIR`、`STANDALONE_COMPILE/STALLED`。

- [x] 更新工作台相关验收文档
  - 状态：已完成
  - 范围：`docs/项目全流程真实验收手册.md`、`docs/项目启动配置清单.md` 中的工作台观察说明。
  - 已完成：已补充资料源处理历史、统一处理任务真链路、空 schema 临时关闭 deep research 启动校验的工作台验收口径。
  - 验收依据：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AdminProcessingTaskControllerTests,AdminUploadControllerTests,ManagementJsRuntimeTests,AdminPageControllerTests test`，16 个测试通过；文档关键字检查确认运行文档使用“处理历史”新口径。

---

## 6. 主线 E：统一验收与清理

- [x] 建立阶段验收命令清单
  - 状态：已完成
  - 要求：每条主线至少有自动化测试、真实链路验收或人工核验步骤。
  - 阶段命令：
    - B0 / 结构化资料 MVP：`QUERY_SUMMARY_TSV=.codex/run/card-regression-query-summary-20260505.tsv scripts/run-b1-structured-regression.sh`
    - C1 / 人工复核闭环：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=ArticleManualReviewServiceTests,ArticleReviewAuditJdbcRepositoryTests,AdminArticleQueryServiceTests,AdminGovernanceApiIntegrationTests,ManagementJsRuntimeTests,AdminPageControllerTests,AdminSourceControllerTests test`
    - C3 / 风险抽检：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AdminArticleQueryServiceTests,StatusServiceTests,ManagementJsRuntimeTests,AdminPageControllerTests,ArticleJdbcRepositoryTests,AdminGovernanceApiIntegrationTests,ArticleManualReviewServiceTests,LatticeHttpClientTests,LatticeMcpToolsB7Test test`
    - C4 / 结果反馈：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AdminGovernanceApiIntegrationTests,ArticleManualReviewServiceTests,LatticeHttpClientTests,LatticeMcpToolsB7Test,AnswerFeedbackServiceTests,StatusServiceTests,AskJsRuntimeTests,ManagementJsRuntimeTests,AdminPageControllerTests,AnswerFeedbackJdbcRepositoryTests test`
    - C5 / 热点抽检：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=ArticleUsageStatsJdbcRepositoryTests,AdminGovernanceApiIntegrationTests,ManagementJsRuntimeTests,AdminPageControllerTests test`
    - D / 工作台处理任务：`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AdminProcessingTaskControllerTests,AdminUploadControllerTests,ManagementJsRuntimeTests,AdminPageControllerTests test`
  - 真实链路补验：D2 使用 18082 独立 schema `lattice_d2_processing_real_20260506`，验收产物 `/tmp/lattice-d2-processing-real-20260506/processing-tasks-after-server-and-stalled.json`。
  - 本轮完成口径：A1/A2/A3/A4、B0 MVP、B1/B2/B3、C1/C3/C4/C5、D、E 均纳入本期并已完成；B0 后置增强与 C6 backlog 不纳入本轮完成率。

- [x] 建立真实回归产物目录规范
  - 状态：已完成
  - 建议：`.codex/run/*` 只放临时验收产物，关键结论回写本文。
  - 目录规范：
    - `.codex/run/<scope>-<yyyymmdd>/`：保存可复跑或需长期对照的本地回归摘要、截图、TSV、JSON。
    - `/tmp/lattice-<scope>-<yyyymmdd>/`：保存一次性 HTTP / 浏览器 / 临时 schema 验收产物。
    - 计划文件只写结论、schema、端口、关键文件名和最终状态，不粘贴大段响应。
    - 临时实例完成后必须停掉，避免占用 `18082` 或污染共享 schema。
  - 本轮落点：D2 真链路产物保留在 `/tmp/lattice-d2-processing-real-20260506`，关键结论已回写本文。

- [x] 每轮开发后执行防特判扫描
  - 状态：已完成
  - 范围：`src/main/java`、必要时包含 `src/test/java`。
  - 验收：主链不得出现业务专名、特定文件名、特定题号、固定答案模板。
  - 已完成：主链与 admin 静态资源扫描未命中 `scenario_id / case_num / step_index / scenarios.xlsx / 特定题号 / 卡券样例词 / 固定接口路径`；原 ask 页业务化 placeholder 已改为通用资料问题。
  - 验收依据：`rg -n "scenario_id|case_num|step_index|scenarios\\.xlsx|100912|100978|100979|100914|卡券|SVC|Ebuy|PNC|S4-922|S4-822|/onlineLifecycle|/cashier|/dpfm|payment_order|优惠|退款" src/main/java src/main/resources/static/admin` 空结果；`mvn -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AskJsRuntimeTests,AdminPageControllerTests test`，3 个测试通过。

- [x] 完成最终收口说明
  - 状态：已完成
  - 内容：哪些主线已完成、哪些仍是风险、哪些进入 backlog、真实验收结果是什么。
  - 本轮已完成：`1.1` 本期 P0、A1/A2/A3/A4、B0 MVP、B1/B2/B3、C1/C3/C4/C5、D1/D2/D3、E1/E2/E3/E4 均已完成，并有自动化、真实链路或 benchmark 证据。
  - 本轮真实验收：A1 固定 Query 回归、A2 compile 基线、A2 PDF 抽取专项、C1/C3/C4/D2 均已补验；D2 最终确认 `SOURCE_SYNC / UPLOAD`、`SOURCE_SYNC / SERVER_DIR`、`STANDALONE_COMPILE / STALLED` 均可见。
  - 本轮质量门禁：结构化问答统一回归、内容治理组合回归、工作台组合回归、A3 dispatcher 组合回归、A4 gap report / resume recovery、最终编译、防特判扫描均通过；主链和 admin 静态资源未发现特定文档、特定题号、样例列名或固定业务答案特判。
  - 已有超越证据：`AstCitationDeepResearchBenchmarkRunner#shouldGenerateGapReport` 已生成 Java / TS 对比与证据矩阵，`blockingGaps=[]`，共享 benchmark、结构化事实、retrieval recall、真实问题回放、恢复能力、默认配置收益均为 `LEADING`。
  - 剩余风险：当前已取消 Flyway 和独立 schema 口径；真实 UI / Query 回归前先执行 `./scripts/reset-lattice-schema.sh` 重建唯一 `lattice` schema，再重新导入资料后复验。
  - 后置增强：B0 的 JSON/YAML、Markdown 表格、跨 sheet 关联、高级排序语义、数值高级聚合、后台结构化 UI 仍为后续范围；C6 治理增强 backlog 不纳入本轮完成率。
  - 下一轮建议：如继续推进，先在干净 schema 上重新导入 `docs/scenarios.xlsx`、指定 PDF 与核心项目文档，跑完整 Query 回放 manifest；再进入 B0 后置增强或 C6 治理增强。继续坚持通用结构化证据、通用检索审计和通用提示词约束，不为单份资料或样例列名补特判。

- [x] 干净 schema 18082 真实 Query 回放复验
  - 状态：已完成
  - 目标：该项为历史隔离 schema 复验记录；当前后续复验统一在手动重建后的 `lattice` schema 上执行。
  - 当前进展：历史记录曾使用 `lattice_query_replay_20260506` 隔离 schema；当前已废弃该口径，不再新增 `lattice_*` schema。
  - 当前进展：仅复制旧 `lattice` 的运行配置（LLM 连接、模型、绑定、query vector setting），未复制知识数据；原始运行配置 dump 已删除，仅保留脱敏摘要与计数。
  - 当前进展：已导入 `/tmp/lattice-query-replay-import-20260506`，包含 `docs/scenarios.xlsx`、指定 PDF、`docs/卡券三期-迁移方案.md`、`docs/项目启动配置清单.md`、`README.md`；compile job=`2ebfda73-fd42-4d0d-bcb0-07463fb0c366` 正在 `compile_new_articles`。
  - 已验证：`source_files=5`、`structured_tables=2` 已落库；结构化行定位题 `scenario_id=100912` 在干净 schema 上返回 `case_num=100714`，聚合统计题按 `category` 返回 `RULE_BASED / STRUCTURED_QUERY`，均 `PASSED`。
  - 当前进展：已跑完整 `scripts/run-query-regression.mjs` manifest，10 题 9 PASS；唯一失败为 `Q-EXACT-PATH-001`，答案内容、检索命中和 projection 均正确，但同一段尾随引用拆成 4 个 claim 后有 2 个 claim 因引用覆盖率偏严被 demote，`citationCoverage=0.5`。
  - 已完成：按通用 citation 规则修复，不写业务词、文件名或路径特判；split claim 的 citation 保留原始段落上下文，单句有硬事实命中且无未命中严格事实时，可用同段上下文完成引用验证。
  - 验收依据：新增匿名 demo path/接口契约回归，验证同段尾随引用可支撑多 claim，同时验证新增未支撑严格事实的 claim 仍会 demote；`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=CitationExtractorTests,CitationValidatorTests,CitationCheckServiceTests,QueryResponseCitationAssemblerTests test` 通过。
  - 验收依据：Query 组合回归 `mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AnswerGenerationServiceTests,QueryPreparationServiceTests,SourceSearchServiceTests,QueryControllerTests,QueryGraphOrchestratorTests,CitationExtractorTests,CitationValidatorTests,CitationCheckServiceTests,QueryResponseCitationAssemblerTests,DeepResearchResearcherServiceTests test` 通过。
  - 验收依据：18082 已用同一干净 schema 重启到新代码，完整回放 `.codex/run/query-replay-clean-20260506/full-query-regression-after-citation-fix-rerun/query_metrics.json` 为 10/10 PASS，`casePassRate=1`、`httpFailureRate=0`、`timeoutRate=0`、`fallbackRate=0`、`llmSuccessRate=0.4`、`recallAt5=1`、`recallAt10=1`、`citationPrecision=0.9375`。
  - 已验证：`Q-EXACT-PATH-001` 在新代码下 `generationMode=LLM / modelExecutionStatus=SUCCESS / citationCoverage=1 / verified=4 / demoted=0`；此前失败的“迁移后 path 不可以改”和“上游零代码改造”均通过通用 context overlap 验证。
  - 备注：中途一次完整回放里 `Q-MQ-BOUNDARY-001` 因模型结构化输出偶发未被解析而进入 `LLM_OUTPUT_INVALID` fallback；单题 fresh rerun 与最终完整 rerun 均恢复为 `LLM/SUCCESS`。后续如继续收敛稳定性，应优先增强通用结构化 JSON/Markdown 解析容错，而不是为该题补样例特判。
  - 备注：当前 18082 仍运行在 `lattice_query_replay_20260506` schema，compile worker 已禁用以避免继续长时间 Writer 调用；compile job 当前记录为 `QUEUED / compile_new_articles / 39/82`。本轮 Query 验收基于 `source_files=5`、`source_file_chunks=2584`、`structured_tables=2`、`structured_table_rows=1542`、`structured_table_cells=15069`、`fact_cards=1060` 完成，全量 article compile 不纳入本项完成口径。
  - 进行中：用户明确要求撤回“为当前资料/题集精修”的特殊逻辑；当前方向改为只修通用检索执行层，重点排查 FTS + LIKE token 预算导致长文本/PDF 证据通道超时或弱召回的问题，不新增业务词、文档名、样例值、题型问法词白名单。
  - 已完成：通用 lexical 检索执行预算已收口；source file、source chunk、article chunk、fact card、contribution、ref key 的宽文本 `LIKE` 统一只使用有限个高信号 token，FTS 仍使用完整问题；同时清理主链里残留的“第几批/哪一批”问法词触发，不新增业务词、文档名、样例值或固定答案模板。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=LexicalSearchTokenBudgetTests,SourceFileChunkJdbcRepositoryTests,FactCardJdbcRepositoryTests,ArticleChunkJdbcRepositoryTests,SourceFileJdbcRepositoryTests,ContributionJdbcRepositoryTests test` 通过。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=LexicalSearchTokenBudgetTests,QueryIntentClassifierTests,AnswerGenerationServiceTests,QueryGraphOrchestratorTests,FactCardFtsSearchServiceTests,SourceSearchServiceTests test` 通过；`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -DskipTests compile` 通过。
  - 验收依据：防特判扫描 `rg -n "scenario_id|case_num|step_index|scenarios\\.xlsx|100912|100978|100979|100914|美团|充电宝|卡券三期|/api/v2/fulfillment/request/return|featureToggleList|权威配置|硬编码|新增值|第几批|哪一批|迁移后" src/main/java src/main/resources/static/admin scripts --glob '!target/**'` 仅命中守门脚本自身 `BLOCKED_PATTERN` 和无关配置注释，未命中 Query / 检索 / 回答主链特殊逻辑。
  - 已验证：18082 已在 `lattice` schema 启动，三资料真实验收 `.codex/run/answer-accuracy-202605071245-token-budget/summary.json` 为 4/8 PASS；XLSX 3/3，Markdown 接口题事实正确但严格断言缺同义词，PDF 3 题仍未过但已无超时、无结构化 COUNT 误触发。
  - 进行中：继续修通用证据覆盖、数值表达与 `LLM_OUTPUT_INVALID` fallback 证据选择；不得新增业务词、文档名、样例值、题型问法词白名单。
  - 进行中：已撤除本轮新增中文停用词 / 问法清洗式 token 逻辑，改为通用形态分组与位置均衡抽样；fallback 已补通用 JSON 值展开、公式原值提示、对比两侧证据保留与流程候选优先。
  - 验收依据：`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=QueryTokenExtractorTests,AnswerGenerationServiceTests,QueryGraphOrchestratorTests,FactCardFtsSearchServiceTests,SourceSearchServiceTests test` 通过；`mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -DskipTests compile` 通过；`git diff --check` 通过。
  - 已验证：三资料真实验收 `.codex/run/answer-accuracy-202605070547-general-fix/summary.json` 提升为 5/8 PASS；Markdown 2/2，XLSX 2/3，PDF 1/3。剩余待修为结构化多条件行定位后投影收口、PDF 原始 source chunk 多焦点覆盖、PDF 券账单公式与发票证据选择，仍禁止新增业务词、文档名、样例值或固定答案模板。
  - 进行中：已按用户反馈撤除 `RrfFusionService` 中基于“是否/怎么/公式/字段/值”等问法词触发直接证据保护的逻辑；当前保护条件改为只比较直接证据对问题高信号 token 的覆盖分与背景文章覆盖分，下一步重启 18082 后复跑三资料验收。

---

## 7. 已合并并删除的旧来源

以下旧文件内容已合并到本文，后续不再维护：

- `docs/Java版全面超越原版实施设计方案.md`
- `docs/Java版全面超越原版重构实施拆解清单.md`
- `docs/Java版全面超越原版优化收口清单.md`
- `docs/Query多通道并发召回技术方案.md`
- `docs/复杂文档结构化证据驱动问答增强方案.md`
- `docs/plans/2026-04-30-query验收与问答收口.md`
- `docs/plans/2026-05-04-复杂文档结构化证据驱动问答增强实施清单.md`
- `docs/已入库内容人工复核闭环改造清单.md`
- `docs/已入库内容风险抽检与结果驱动复核方案.md`
- `docs/工作台当前处理任务统一可见性改造方案.md`
