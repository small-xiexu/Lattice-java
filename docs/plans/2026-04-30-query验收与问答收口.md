# 2026-04-30 Query 验收与问答收口

- [x] 停止当前项目运行实例并确认本轮复验端口/依赖基线
  - 验收：已停止原 `18082` Spring Boot 实例；`1808x/1809x/1810x` 端口当前无残留监听。依赖继续复用 `vector_db(5432)` 与 `redis(6379)`。
- [x] 清理相关业务库并按当前基线重建 fresh schema
  - 验收：已删除旧现场 `lattice_pdf_eval_20260429`，新建 `lattice_pdf_eval_20260430`，并在 `http://127.0.0.1:18082` 启动 fresh 实例；Flyway 已成功迁移 `V1__baseline_schema.sql`。
- [x] 按 `.claude/t1.md` 配置模型中心、向量与 OCR 路由
  - 验收：已在 clean schema `lattice_clean_eval_20260501` 通过 Admin API 重建 `LLM/OCR/vector` 配置；`deepseek`、`mimo`、`textin_xparse` 连接探测通过，向量状态为 `vectorEnabled=true / indexedArticleCount=55 / annIndexReady=true`。
  - 已验证：已修复 `embedding-main` 的 connection probe 误走 `/models` 路径问题；当前 `connectionId=3` 会正确探测 `/embeddings`，`modelId=3` 也已真实返回 `2000` 维向量。
- [x] 以保留的中性样本完成 source sync、compile、query/feedback 全链路真实验收
  - 验收：`docs/test/scenarios.xlsx` 已在 `http://127.0.0.1:18082` 完成 `source sync -> compile -> vector rebuild -> query/feedback` 全链路；`source-runs/1` 为 `SUCCEEDED`，总览为 `articleCount=55 / sourceFileCount=1 / contributionCount=1 / pendingQueryCount=0`。
  - 已验证：完成 10 题 query 抽样验收（基于 PDF 原文人工核对标准答案），其中 `7` 题正确、`3` 题部分正确，无完全跑飞但存在关键细节漏答；严格准确率约 `70%`，按部分正确折半计分约 `85%`。本轮新增 pending 已全部 `discard`，队列计数已从 `65` 恢复到原基线 `28`。
  - 待收口：当前主要偏差集中在 `场景编号重排/旧新口径混用`、`显式规则原文抽取不足（如 MQ topic 命名规范）`、`同一文档内后续澄清结论覆盖前文旧表述` 三类问题，尚不能宣称“配置类/结构类问题已稳定高准确”。
- [x] 根据验收结果打磨问答准确度并补齐必要回归测试
  - 进行中：`2026-05-02` 当前已转入压降 `needsHumanReview`，正在抽样分析剩余 `45` 条未通过 article 的共性模式，优先定位 compile/review 产物中的系统性问题。
  - 进行中：`2026-05-02` 正在基于当前收紧后的 review/prompt 规则重跑 `docs/test/scenarios.xlsx` 的 compile 与 query 抽样，验证 `passed / needs_human_review` 比例和真实问答准确率是否改善。
  - 进行中：已转向 compile/review 智能体提示词与审查约束优化，目标是降低“证据不足却生成推断型 article”比例，优先收紧 writer / reviewer 的无证据输出策略。
  - 进行中：`2026-05-02` 正在重新梳理 compile/review 代码链路，优先核对 writer / reviewer / fixer / review parser / fallback 的职责边界，避免只改 prompt 却遗漏状态判定和兜底放行问题。
  - 已验证：已先收紧 review fallback 语义，`parseFailed` 与 `timeoutFallback` 不再视为自动通过；对应最小回归已通过，避免 reviewer 解析失败/超时后把低质量稿件误标为 `passed`。
  - 已验证：已收紧 writer / reviewer / fixer 的基础提示词约束，明确“证据不足的异常场景只能写证据缺口，不得补成精确事实”；对应 `SchemaAwarePromptsTests / ReviewResultParserTests / ReviewerAgentTests / CompileArticleReviewFlowTests / ArticleReviewerGatewayTests` 最小回归全部通过。
  - 验收：已完成主链与测试基座的特判清理、真实样本下线，并将 `AGENTS.md` 升级为“零容忍特判回流”禁令。
  - 已验证：针对 `IAG / SWIP / 广发 / 买一赠一 / bogo / SVC卡 / 卡券三期 / businessTypeCode=26 / 场景 8 / 低空经济 / 银行星礼包 / SRkit / b2bSRKit / CgbPay` 等关键词执行全仓复扫，在 `src/main/java`、`src/test/java`、`docs`、`README.md`、`AGENTS.md` 中未发现新的主链特判实现或业务专名分支；剩余命中仅为本计划台账中的历史记录说明。
  - 已验证：定向回归累计 `73/73` 通过；高置信结构题（如记录数、阶段校验、退款冲正表）已能稳定返回 `generationMode=LLM / modelExecutionStatus=SUCCESS / reviewStatus=PASSED`。
  - 结论：部分 `passed` article 本身仍含“推断”内容，暂不能把所有异常题都按“高准确事实题”口径计算；后续应优先继续打磨 compile 抽取与 review 规则，而不是在 query 主链补特判。
- [x] 复跑关键自动化与真实链路，回写最终结论与遗留项
  - 已验证：`2026-05-02` 已按最新代码重启 `http://127.0.0.1:18082` 并完成整体验证；健康检查 `UP`，`/api/v1/admin/processing-tasks` 已收敛为 `运行中 / 待确认 / 已完成 / 失败` 四态展示，卡片内阶段词为 `编译中 / 审查中 / 修复中 / 收口中`。
  - 已验证：资料源同步历史已压缩为 4 步展示，不再铺满 20+ 内部步骤；`SKIPPED_NO_CHANGE` 归到 `已完成`，`STALLED` 归到 `失败`，并在历史详情中保留具体说明文案。
  - 验收：clean schema `lattice_clean_eval_20260501` 已完成 source sync、compile、vector rebuild、HTTP query、pending `confirm/discard` 闭环；数据库侧确认 `pending_queries=0`、`contributions=1`。
  - 已验证：重启后的 clean schema 当前统计为 `articleCount=110 / passedArticles=65 / needsHumanReviewArticles=45 / pending_queries=0 / contributions=1`；说明本轮收紧 review/prompt 后，`passed` 条目已从上一轮的 `10` 提升到 `65`。
  - 已验证：重启后真实 `/api/v1/query` 抽样 2 题继续返回 `generationMode=LLM / modelExecutionStatus=SUCCESS / reviewStatus=PASSED / answerOutcome=SUCCESS`，并已完成 `discard` 收口。
  - 遗留：`needs_human_reviewArticles=45/110` 仍不低，说明 `scenarios.xlsx` 里仍有一批低置信或推断型 article；后续若继续追求更高准确率，应继续打磨 compile 抽取、review 约束与 evidence 组织，而不是在 query 主链补特判。
