# 增量编译粒度收紧实施清单

## 1. 目标

基于 `2026-04-18` 真实验收里“只修改 `ops/postmortem.md` 但 `persistedCount=9`”这一实际限制，按“源文件命中 + 依赖图传播”口径收紧增量编译受影响范围，完成：

- 让增量编译先按变更源文件定位直接命中的文章集合
- 再按依赖图补齐真正受影响的下游文章集合
- 让落库、chunk 替换、向量刷新只作用于最小必要文章集合
- 保持增量问答结果正确，不因粒度收紧漏掉真实受影响文章

## 2. 范围

- 文档台账：
  - `docs/plans/增量编译粒度收紧实施清单.md`
- 增量编译主链：
  - `src/main/java/com/xbk/lattice/compiler/service/IncrementalCompileService.java`
- 依赖图 / 传播辅助：
  - `src/main/java/com/xbk/lattice/governance/DependencyGraphService.java`
  - `src/main/java/com/xbk/lattice/governance/PropagationService.java`
- 定向测试：
  - `src/test/java/com/xbk/lattice/compiler/service/IncrementalCompileServiceTests.java`
  - `src/test/java/com/xbk/lattice/compiler/service/StateGraphCompileOrchestratorTests.java`

## 3. 非目标

- 本轮不做“LLM 拥有最终排除权”的增量裁决方案
- 本轮不扩大到 repo diff / rollback 真实回归
- 本轮不重写整套 compile graph，只聚焦受影响文章集合收紧

## 4. 实施清单

- [x] I1 建立“变更源文件 -> 直接命中文章”最小候选集
  - 验收：已在 `IncrementalCompileService` 与 `IngestSourcesNode` 接入基于 `source_files` 的源文件内容差异筛选，整目录增量时仅保留新增或内容变化的 `rawSources`；新增“同目录仅 1 个文件变化”单测与 StateGraph 集成测均通过。

- [x] I2 基于依赖图补齐真实下游受影响文章
  - 验收：已接入依赖图传播，把直命中文章的下游文章并入增强集合，并通过 `IncrementalCompileServiceTests` 与 `StateGraphCompileOrchestratorTests` 的下游传播场景。

- [x] I3 让增量落库 / chunk / 向量刷新只处理最小受影响集合
  - 验收：先对 `/tmp/lattice-real-e2e-20260422-baseline.9NOvdu` 执行全量基线（`jobId=3450e531-166f-4dcd-9625-fafb86eb839d`，`persistedCount=9`），再对 `/tmp/lattice-real-e2e-20260418-src` 执行增量（`jobId=1ca87935-8dcc-4a85-a4b9-04ee2e1c0652`，`persistedCount=4`）；真实目录回归已明显低于历史 `9`，落库、chunk 与向量刷新范围已收紧。

- [x] I4 补齐定向测试与回归口径
  - 验收：`mvn -s .codex/maven-settings.xml -Dtest=IncrementalCompileServiceTests,StateGraphCompileOrchestratorTests test` 与 `mvn -s .codex/maven-settings.xml -Dtest=CompilePipelineVectorIndexingTests test` 均通过。

## 5. 验收口径

- 单文件增量修改时，`persistedCount` 明显低于当前真实验收中的 `9`
- 未命中且不在依赖传播链路上的文章，不应被重新落库
- 直接命中文章与真实下游文章仍能拿到正确更新
- 增量编译相关定向测试通过，且至少覆盖“直接命中”“下游传播”“无关文章不刷新”三类场景

## 6. 当前断点

- 当前阶段：本轮“增量编译粒度收紧”实施与真实回归已完成
- 已知约束：
  - 本轮采用“规则主导”方案，最终受影响集合以源文件命中、源文件内容差异筛选与依赖图传播为准
  - 工作区存在用户未提交代码改动，本轮只在增量编译相关范围内推进，未回退其他改动
- 下一步：若还要继续把真实 `persistedCount=4` 进一步压缩，需要再分析这 4 篇文章中的传播链与聚合文章设计，但这已不阻塞本轮验收。
