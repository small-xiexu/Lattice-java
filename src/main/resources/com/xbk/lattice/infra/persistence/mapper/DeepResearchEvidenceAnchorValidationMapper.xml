<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xbk.lattice.infra.persistence.mapper.DeepResearchEvidenceAnchorValidationMapper">

    <insert id="insert">
        insert into deep_research_evidence_anchor_validations (
            run_id, anchor_id, validation_round, validation_status,
            validated_by, reason, matched_excerpt
        )
        values (
            #{record.runId},
            #{record.anchorId},
            #{record.validationRound},
            #{record.validationStatus},
            #{record.validatedBy},
            #{record.reason},
            #{record.matchedExcerpt}
        )
    </insert>
</mapper>
