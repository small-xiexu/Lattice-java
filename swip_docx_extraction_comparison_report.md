# SWIP docx 解析方式对比实验报告

- **报告时间**：2026-05-14
- **实验目的**：比较多种只读解析方式对 SWIP docx 的提取效果，确认 poi_xwpf 是否为根因，给出下一轮最小修复方案
- **源文件**：同前两轮，未修改

---

## 1. Redline 检查

| 指标 | 值 |
|---|---|
| BLOCKER | **0** |

结论：可以继续。

---

## 2. 参与对比的解析方式

| 方式 | 类型 | 说明 |
|---|---|---|
| **poi_xwpf** | Java 库（项目内置） | 当前 Lattice 使用的解析器，`parseProvider: "poi_xwpf"` |
| **textutil** | macOS 系统工具 | `textutil -convert txt`，macOS 原生 docx→txt 转换 |
| **OpenXML** | Python 直接解析 | `python3 -c` 逐 paragraph/heading/table 提取，无依赖 |

pandoc 和 LibreOffice 均未安装，不参与对比。

---

## 3. 字符数对比

| 文档 | poi_xwpf (DB) | textutil | OpenXML | textutil vs poi | OpenXML vs poi |
|---|---|---|---|---|---|
| 使用手册 (10.3MB) | **4,510** | 11,290 | 10,674 | **2.50x** | **2.37x** |
| 安装手册 (12.9MB) | **9,153** | 20,867 | 19,986 | **2.28x** | **2.18x** |
| **合计** | **13,663** | **32,157** | **30,660** | **2.35x** | **2.24x** |

结论：poi_xwpf 仅提取到 textutil 的 42%，是文本抽取不足的**直接根因**。

---

## 4. 关键章节覆盖矩阵

以下章节在 poi_xwpf 提取中被标记为"不足"或"严重不足"。下表验证 textutil/OpenXML 是否覆盖。

### 4.1 使用手册（12 个 case）

| 章节 | 对应 case | poi_xwpf | textutil | OpenXML | 覆盖验证 |
|---|---|---|---|---|---|
| §1 系统目标（含"监控"） | GOAL-001 | 仅标题 | **完整** | **完整** | textutil/OpenXML 包含完整段落 |
| §2.2.2 银行卡退款（输入参考号+日期） | BANK-REFUND-001 | **严重不足** | **完整** | **完整** | 均有详细操作步骤 |
| §2.2.3 银行卡结算（时机+结果） | BANK-SETTLEMENT-001 | 不足 | **完整** | **完整** | 均有"每日日结后/打印小票" |
| §2.2.5 重印（按流水号/末笔） | REPRINT-001 | 中等 | **完整** | **完整** | 均有两种重印方式描述 |
| §2.3.1 杉德签到（必须+每天开店前） | SAND-SIGN-001 | 中等 | **完整** | **完整** | 均有"每天开店前执行一次" |
| §2.3.2 杉德手工结算（按卡种+无交易不打印） | SAND-SETTLEMENT-001 | **严重不足** | **完整** | **完整** | 均有"按卡种分别打印/无交易不打印" |
| §2.3.4 得仕卡当日撤销（仅得仕卡+原金额一致） | DESHI-VOID-001 | **严重不足** | **完整** | **完整** | 均有完整撤销步骤 |
| §2.5 银行积分（eBuy+返回处理中重试） | EBUY-SETTLEMENT-001 | 中等 | **完整** | **完整** | 均有"处理中请再次执行" |
| FAQ 系统无响应（SNIFF+HTTPS+IT） | NO-RESPONSE-001 | **严重不足** | **完整** | **完整** | 均有完整排查步骤 |
| FAQ 终止交易（长按 LOGO 10秒） | TERMINATE-STUCK-001 | 足够 | **完整** | **完整** | 均有操作说明 |
| FAQ 打印纸规格（40mm×58mm+供应商） | PRINT-PAPER-001 | **严重不足** | **完整** | **完整** | 均有"直径40mm×长度58mm" |

### 4.2 安装手册（9 个 case）

| 章节 | 对应 case | poi_xwpf | textutil | OpenXML | 覆盖验证 |
|---|---|---|---|---|---|
| §3.2.1 入门店内网（IP后缀149/150） | IP-SUFFIX-001 | 中等 | **完整** | **完整** | 均有"149→151→150调整步骤" |
| §3.2.2 安装APP（9个APP完整列表） | APP-LIST-001 | **严重不足** | **完整** | **完整** | 均有全部9个APP名称 |
| §3.3 POS DLL 初始化顺序 | DLL-ORDER-001 | **严重不足** | **完整** | **完整** | 均有各渠道DLL顺序 |
| §3.3.1 交易密钥（swip-keys.p13+一机一密） | KEY-FILE-001 | 轻微 | **完整** | **完整** | 均有"一机一密"描述 |
| §3.3.5 swip-pos.json 配置 | SWIP-POS-JSON-001 | 部分 | **完整** | **完整** | 均有JSON结构和字段说明 |
| §4.4 APP升级影响（按APP分组） | APP-UPGRADE-IMPACT-001 | **严重不足** | **完整** | **完整** | 均有5个APP各自的升级影响说明 |
| §4.8 证书自动更新（提前51/31天+11点） | CERT-UPDATE-001 | **严重不足** | **完整** | **完整** | 均有"提前51/31天晚上11点" |
| §4.9-4.10 日志访问（9999+6666+目录） | LOGS-001 | **严重不足** | **完整** | **完整** | 均有完整日志路径和目录结构 |
| §3.2 证书命名规则 | CERT-NAMING-001 | 部分 | **完整** | **完整** | 均有两个证书命名规则 |

### 4.3 覆盖率汇总

| 方式 | 严重不足章节覆盖 | 不足章节覆盖 | 总体文本量 |
|---|---|---|---|
| poi_xwpf | 0 / 11 | 3 / 20 | 13,663 |
| textutil | **20 / 20** | **20 / 20** | 32,157 |
| OpenXML | **20 / 20** | **20 / 20** | 30,660 |

---

## 5. 图片与 OCR 需求

### 5.1 图片数量

| 文档 | 检测图片数 | 典型截图内容 |
|---|---|---|
| 使用手册 | 71 张 | POS操作截图、支付界面、签购单样例、故障截图 |
| 安装手册 | 64 张 | 系统设置截图、证书安装界面、APP安装进度、日志目录 |
| **合计** | **135 张** | |

### 5.2 OCR 需求评估

- 截图中的信息（如：界面按钮文字、状态提示、错误码）在 textutil/OpenXML 纯文本提取中**仍然不可获取**
- 但正文中已包含大部分操作步骤的文字描述，截图多为辅助说明
- 约 **3-5 个 case**（LOGS-001、BANK-REFUND-001、DLL-ORDER-001）可能仍需截图中的文字来补充细节
- 由于当前 Lattice 不启用 OCR（`ocrApplied: false`），即使更换解析器，截图内容仍不被提取

结论：OCR 需求**存在但优先级低于更换解析器**。先提升文本提取量（2.35x），再评估是否仍需 OCR。

---

## 6. 表格提取

| 方式 | 使用手册 | 安装手册 | 提取质量 |
|---|---|---|---|
| poi_xwpf | 0 个表格 | 0 个表格 | 不提取表格 |
| textutil | 行内文本 | 行内文本 | 表格内容作为文本保留，但失去行列结构 |
| OpenXML | 0 个（代码未处理） | 2 个（489 chars） | 可通过扩展代码提取更多 |

安装手册中检测到的 2 个表格包含：错误码对照表（杉德/资和信/易百），这是 FAQ case 的关键参考数据。

---

## 7. 三种方式对比总结

| 维度 | poi_xwpf | textutil | OpenXML |
|---|---|---|---|
| 总字符数 | 13,663 | **32,157** | 30,660 |
| 相对 poi 倍数 | 1x | **2.35x** | 2.24x |
| 章节覆盖 | 严重不足 | **完整** | **完整** |
| 表格提取 | 无 | 行内文本（弱） | 结构化提取（强） |
| 图片提取 | 无 | 无 | 无（可统计数量） |
| 外部依赖 | 项目已集成 | macOS 系统工具 | Python 3 + xml.etree（标准库） |
| 集成难度 | 已集成 | 需预处理步骤 | 需预处理步骤 |
| 运行环境限制 | 无 | 仅 macOS | 跨平台 |

---

## 8. 下一轮最小修复方案

### 推荐方案：textutil 预处理 + 纯文本导入

**做法**：在 Lattice 摄取前，用 `textutil -convert txt` 将 docx 转为 `.txt`，再将 txt 作为 source 文件导入 Lattice。

**理由**：
1. **文本量提升 2.35x**（13.6K → 32.2K），使 Topic Planner 能将两份文档切分成 **10-20 个 topic**（当前仅 4 个）
2. **覆盖所有 20 个关键章节**，之前标记为"严重不足"的 11 个章节全部可获取
3. 零外部依赖安装，macOS 原生自带
4. 对 Lattice 代码零修改——只需在导入前做一次文件格式转换
5. 对 Lattice 配置零修改
6. 不接入 baseline suite，不修改题集

**操作步骤**（均在 SWIP 隔离环境内）：
1. `textutil -convert txt` 将两份 docx 转为 txt
2. 重新上传 txt 文件到 Lattice（重建 SWIP schema）
3. 重新编译（LLM Writer 自动根据更充足的文本生成更多 article）
4. 重新运行 SWIP eval，对比 pass rate

### 备选方案：OpenXML 直接解析

如果未来需要跨平台支持（Linux/Windows），可选用 OpenXML 直接解析。优势是可获取结构化信息（章节层级、表格数据、图片引用），但需要：
- 编写 docx→markdown 的预处理脚本
- 在编译流程中增加预处理步骤

### 暂不推荐

- **OCR 方案**：当前阶段不需要。先验证文本提取提升后的效果，如果部分截图密集型 case 仍然失败，再考虑 OCR
- **调整 topic splitter 配置**：不解决根本问题。即使降低 `min-topic-chars`，poi_xwpf 提取出的原始文本量也太少，无法生出足够的内容

---

## 9. 合规声明

| 项 | 状态 |
|---|---|
| 是否修改源代码（src/main/java/**） | **否** |
| 是否修改测试代码（src/test/java/**） | **否** |
| 是否修改配置（src/main/resources/**） | **否** |
| 是否修改题集（docs/test/swip-query-eval-candidates.json） | **否** |
| 是否修改 regression suite | **否** |
| 是否污染主 baseline（ai-rag-knowledge） | **否** |
| 是否提交代码 | **否** |
| 本轮对比实验使用的是否为只读方式 | **是**（textutil 读文件、Python 读文件、SQL 只读查询） |
