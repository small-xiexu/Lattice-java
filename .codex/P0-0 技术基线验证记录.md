# P0-0 技术基线验证记录

**文档版本**：v0.1  
**创建日期**：2026-04-14  
**所属阶段**：B0 技术基线 Spike  
**对应方案**：[`实施方案.md`](/Users/sxie/xbk/Lattice-java/.codex/实施方案.md)  
**当前状态**：进行中

---

## 1. 目标

在进入 B1 最小编译闭环前，先锁定以下高不确定性技术点：

1. Spring Boot 3.x 工程骨架是否可稳定启动
2. OpenAI / Anthropic ChatModel 是否可装配
3. Spring AI MCP Server 的 `@McpTool` 是否可被发现
4. Spring AI Alibaba Graph 是否存在可运行的最小链路
5. PostgreSQL 16 + `pgvector` 是否可用
6. 中文检索最小方案是否可执行
7. Embedding 模型与向量维度是否应固定为 `text-embedding-3-small + vector(1536)`

---

## 2. 记录规范

- `烟测结果` 仅允许填写：`PENDING` / `PASS` / `FAIL` / `BLOCKED`
- `最终 artifact`、`version`、`管理方式` 在未锁定前可先写 `待确认`
- 若结果为 `FAIL` 或 `BLOCKED`，必须在 `备注` 中写 fallback 或阻塞原因
- 未真实执行的项不得填写 `PASS`

---

## 3. 依赖锁定台账

| 能力项 | 候选 artifact | 最终 artifact | version | 管理方式(BOM/显式) | 烟测结果 | 备注 |
|------|---------------|---------------|---------|--------------------|---------|------|
| Spring Boot Parent | `org.springframework.boot:spring-boot-starter-parent` | `org.springframework.boot:spring-boot-starter-parent` | `3.5.1` | Parent | PASS | 已通过 `mvn test` 启动 Spring Boot 上下文 |
| Spring AI BOM | `org.springframework.ai:spring-ai-bom` | `org.springframework.ai:spring-ai-bom` | `1.1.2` | BOM | PASS | 选择与 SAA 1.1.2.0 兼容的版本组合，而非单独追 Spring AI 1.1.4 |
| Spring AI Alibaba Graph | `com.alibaba.cloud.ai:spring-ai-alibaba-graph-core` | `com.alibaba.cloud.ai:spring-ai-alibaba-graph-core` | `1.1.2.0` | BOM | PASS | `GraphSmokeTests` 已通过，证明 Graph 依赖可解析并可执行最小链路 |
| Spring AI Alibaba Agent Framework | `com.alibaba.cloud.ai:spring-ai-alibaba-agent-framework` | `deferred` | `1.1.2.0` | BOM | PENDING | 当前 B0 先验证 Graph，不把 Agent Framework 作为首批必验项 |
| OpenAI ChatModel Starter | `org.springframework.ai:spring-ai-starter-model-openai` | `org.springframework.ai:spring-ai-starter-model-openai` | `1.1.2` | BOM | PASS | `LatticeApplicationTests` 已断言 `OpenAiChatModel` Bean 存在 |
| Anthropic ChatModel Starter | `org.springframework.ai:spring-ai-starter-model-anthropic` | `org.springframework.ai:spring-ai-starter-model-anthropic` | `1.1.2` | BOM | PASS | `LatticeApplicationTests` 已断言 `AnthropicChatModel` Bean 存在 |
| MCP Server WebMVC | `org.springframework.ai:spring-ai-starter-mcp-server-webmvc` | `org.springframework.ai:spring-ai-starter-mcp-server-webmvc` | `1.1.2` | BOM | PASS | 启动日志已出现 `Registered tools: 1`，且 `PingTool` 通过注解扫描注册 |
| Sa-Token Starter | `cn.dev33:sa-token-spring-boot3-starter` | `cn.dev33:sa-token-spring-boot3-starter` | `1.45.0` | 显式 | PENDING | 官方仓库当前为 `v1.45.0`；B0 仅锁坐标，B4 再做真实接入验证 |
| PostgreSQL Driver | `org.postgresql:postgresql` | `org.postgresql:postgresql` | `42.7.7` | Spring Boot Parent 管理 | PASS | `PostgresSmokeTests` 已通过，证明 JDBC 驱动可连接本机 PostgreSQL 并执行 SQL |
| pgvector Java | `com.pgvector:pgvector` | `com.pgvector:pgvector` | `0.1.6` | 显式 | PENDING | 官方仓库已确认 Maven 坐标；但 B1 不默认引入 ORM 向量映射，向量相关 SQL 优先交给 `JdbcTemplate/jOOQ` |

---

## 4. 能力烟测清单

### 4.1 Spring Boot 空应用

| 项目 | 内容 |
|------|------|
| 目标 | 空应用可启动，Spring Context 正常 |
| 状态 | PASS |
| 建议动作 | 把根工程升级为 Spring Boot 3.x，增加最小启动类 |
| 验证命令 | `mvn spring-boot:run` 或 `./mvnw spring-boot:run` |
| 通过标准 | `mvn test` 已成功加载 Spring Boot 上下文；后续可补 `/actuator/health` 运行态验证 |
| 失败 fallback | 若 Boot 版本与 AI 依赖不兼容，先锁兼容版本再推进 |

### 4.2 OpenAI ChatModel Bean 装配

| 项目 | 内容 |
|------|------|
| 目标 | OpenAI ChatModel 可被 Spring Context 正常装配 |
| 状态 | PASS |
| 建议动作 | 引入 OpenAI starter，提供最小配置占位 |
| 验证方式 | 启动时完成 Bean 装配，或以最小测试验证注入 |
| 通过标准 | `LatticeApplicationTests` 已断言 `OpenAiChatModel` Bean 存在 |
| 失败 fallback | 若自动装配不稳定，先降级为手工配置 Bean |

### 4.3 Anthropic ChatModel Bean 装配

| 项目 | 内容 |
|------|------|
| 目标 | Anthropic ChatModel 可被 Spring Context 正常装配 |
| 状态 | PASS |
| 建议动作 | 引入 Anthropic starter，提供最小配置占位 |
| 验证方式 | 启动时完成 Bean 装配，或以最小测试验证注入 |
| 通过标准 | `LatticeApplicationTests` 已断言 `AnthropicChatModel` Bean 存在 |
| 失败 fallback | 若双模型接入复杂，B1/B2 可先单模型推进 |

### 4.4 MCP `@McpTool` 发现

| 项目 | 内容 |
|------|------|
| 目标 | 至少 1 个 `@McpTool` 可被 MCP Server 发现 |
| 状态 | PASS |
| 建议动作 | 增加最小 `PingTool` 或 `EchoTool` |
| 验证方式 | 启动 MCP Server，检查工具注册结果 |
| 通过标准 | 启动日志已输出 `Registered tools: 1` |
| 失败 fallback | 若 MCP 依赖或接线不稳定，推迟到 B4 |

### 4.5 Graph 最小样例

| 项目 | 内容 |
|------|------|
| 目标 | 最小 Graph/DAG 样例可装配并执行 |
| 状态 | PASS |
| 建议动作 | 构造 2-3 个最小节点，验证串联执行 |
| 验证方式 | 本地运行样例链路并输出节点结果 |
| 通过标准 | `GraphSmokeTests` 已通过，`StateGraph -> compile -> invoke` 链路成立 |
| 失败 fallback | B1 先使用普通 Service 编排替代 Graph |

### 4.6 PostgreSQL 16 + `pgvector`

| 项目 | 内容 |
|------|------|
| 目标 | 本地 PostgreSQL 启用 `vector` 扩展并可执行最小 SQL |
| 状态 | PASS |
| 建议动作 | 通过 Docker Compose 拉起 PG，并执行扩展 SQL |
| 验证 SQL | `CREATE EXTENSION IF NOT EXISTS vector;` |
| 通过标准 | 容器内已执行 `CREATE EXTENSION IF NOT EXISTS vector`，并完成 `vector(3)` 建表、插入与 `<=>` 距离查询 |
| 失败 fallback | 优先解决本地镜像/扩展问题，不通过则 B1 暂停 |

### 4.7 中文检索最小方案

| 项目 | 内容 |
|------|------|
| 目标 | 至少验证 1 套可运行的中文检索方案 |
| 状态 | PASS |
| 建议动作 | 先验证基础 FTS + `referential_keywords @>`，再评估扩展分词 |
| 验证 SQL | `to_tsvector(...)`、`plainto_tsquery(...)`、`referential_keywords @> ARRAY[...]` |
| 通过标准 | 已验证 `simple` FTS 可运行，且 `referential_keywords @> ARRAY[...]` 精确匹配成立；v1.0 最小方案可落为 `simple FTS + @>` |
| 失败 fallback | `pg_trgm + referential_keywords 精确匹配 + 可配置 FTS` |

### 4.8 Embedding 模型与维度

| 项目 | 内容 |
|------|------|
| 目标 | 锁定 v1.0 Embedding 模型与向量维度 |
| 状态 | PASS |
| 建议动作 | 先按 `text-embedding-3-small + vector(1536)` 作为默认基线 |
| 验证方式 | 与向量列定义、索引策略、后续迁移影响面联动评审 |
| 通过标准 | v1.0 基线锁定为 `text-embedding-3-small + vector(1536)`；后续若切模型必须联动表结构和索引迁移 |
| 失败 fallback | 若模型策略调整，必须在 B1 前重写表结构假设 |

---

## 5. SQL 烟测项

以下 SQL 在本地环境具备后需逐条执行，并回写结果：

```sql
CREATE EXTENSION IF NOT EXISTS vector;

SELECT to_tsvector('simple', 'FC 退款 超时 配置');
SELECT plainto_tsquery('simple', '退款 配置');

SELECT ARRAY['1210', '1310'] @> ARRAY['1210'];
```

若引入自定义 FTS 配置或中文分词扩展，需补充：

- 配置创建 SQL
- 样本文本
- 命中结果
- 是否满足明确性知识检索要求

---

## 6. 执行顺序建议

1. 锁定 Spring Boot 与 Spring AI 版本
2. 验证 Boot 启动
3. 验证 OpenAI / Anthropic Bean 装配
4. 验证最小 `@McpTool`
5. 验证最小 Graph 样例
6. 验证 PostgreSQL + `pgvector`
7. 验证中文检索最小方案
8. 锁定 Embedding 模型与维度

---

## 7. 当前结论

- 当前状态：进行中
- 已完成：
  - 台账模板已建立，能力项、候选依赖和烟测维度已列出
  - Spring Boot / OpenAI / Anthropic / MCP / Graph 第一轮真实烟测已通过
  - PostgreSQL JDBC 连通性、`vector` 类型、FTS、`@>` 数组命中已通过 `PostgresSmokeTests`
  - 全量 `mvn test` 已通过：`LatticeApplicationTests`、`GraphSmokeTests`、`PostgresSmokeTests`
- 已确认：
  - Spring AI 官方稳定文档当前为 `1.1.4`，并声明支持 Spring Boot `3.4.x` 和 `3.5.x`
  - Spring AI MCP Server WebMVC starter 为 `spring-ai-starter-mcp-server-webmvc`
  - Spring AI OpenAI / Anthropic starter 分别为 `spring-ai-starter-model-openai` 和 `spring-ai-starter-model-anthropic`
  - Spring AI Alibaba 官方版本矩阵当前推荐 `SAA 1.1.2.0 + Spring AI 1.1.2 + Spring Boot 3.5.x`
  - 当前本机默认 `java` / `mvn` 运行在 Java 8，但本机已安装 JDK 21，可切换
  - Maven 全局本地仓库路径为 `/Users/sxie/maven/repository`
  - 本机已存在运行中的 `pgvector/pgvector:pg16` 容器，可直接复用做 B0 SQL 烟测
  - `pgvector-java` 官方 Maven 坐标为 `com.pgvector:pgvector:0.1.6`
  - 官方 README 明确提示：Hibernate 6.4+ 场景优先使用 `hibernate-vector`，不要默认自建 `PGvector` Converter
  - Spring Boot `3.5.1` 管理的 PostgreSQL 驱动版本为 `42.7.7`
  - v1.0 Embedding 基线锁定为 `text-embedding-3-small + vector(1536)`
  - 项目默认应回归使用全局 Maven 配置与全局本地仓库 `/Users/sxie/maven/repository`
  - Sa-Token 官方仓库当前版本为 `v1.45.0`，Spring Boot 3 starter 坐标为 `cn.dev33:sa-token-spring-boot3-starter`
  - Spring Boot `3.5.1` 管理的 Hibernate 版本为 `6.6.18.Final`
  - B1 数据层暂不引入 `hibernate-vector`；向量字段建模、向量写入和向量检索统一后置到 B3，并优先使用 `JdbcTemplate/jOOQ + SQL`
- 进行中：
  - 已发现全局 Maven `settings.xml` 将 `central` 镜像到 `alimaven`；该问题已通过临时项目级 settings 完成隔离验证，但项目级 settings 已删除，不作为长期配置
  - B0 核心烟测已完成，正在收束剩余依赖与实现策略项
  - PostgreSQL 烟测依赖访问本机 `127.0.0.1:5432`，在当前沙箱下需提权执行全量验证
- 待完成：是否把 B0 状态从“进行中”切到“待 B1 启动前复核”
- 下一步：若继续推进，直接进入 B1 最小编译闭环，不在 B1 引入向量 ORM 映射
