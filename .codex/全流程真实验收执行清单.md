# 全流程真实验收执行清单

> 唯一进度台账：本轮“真实启动、复杂知识源编译、复杂查询、后台配置、MCP 工具、项目手册与 README 收口”统一只回写到本文件。

## 验收目标

- [x] 准备真实验收环境并确认依赖可用
- [x] 构造复杂知识源样本并落到本地验收目录
- [x] 完成应用真实启动与健康检查
- [x] 完成后台模型配置中心真实配置与保存
- [x] 完成复杂知识源全量编译与增量编译验收
- [x] 完成复杂查询、纠错、确认、丢弃全链路验收
- [x] 完成 Admin 侧核心功能验收
- [x] 完成 MCP 工具主链路验收
- [x] 完成导出、快照、回滚等高阶能力抽检
- [x] 形成详细项目手册并更新 `README.md`

## 执行记录

- [x] 建立唯一进度台账
  - 验收：已创建本文件，后续全流程验收、文档整理与收口统一回写到此处。
- [x] 读取现有入口文档并确认验收范围
  - 验收：已读取 `AGENTS.md`、`.codex/项目启动配置清单.md`、`README.md`，并补充核对 `CompileController`、`QueryController`、`PendingQueryController`、`AdminLlmConfigController`、`AdminCompileController`、`LatticeMcpTools`、`QueryGraphOrchestrator`、`ArticlePersistSupport` 等实现，确认本轮将覆盖 HTTP、Admin、MCP、复杂文件编译、向量索引、query 重写与纠错闭环。
- [x] 准备隔离验收环境与复杂知识源
  - 已完成：已生成隔离知识源目录 `/tmp/lattice-real-e2e-20260418-src`，覆盖 `md/json/yml/java/pdf/xlsx/drawio/png` 混合输入，并复用 `.codex/real-regression-input` 中现有 PDF / Excel 作为补充样本。
  - 验收：已创建独立 schema `lattice_real_e2e_20260418`；首次启动发现本机 `8080` 已被占用，后续验收实例已切换到 `18080`，避免影响现有服务。
- [x] 完成应用真实启动与健康检查
  - 验收：`mvn -q -s .codex/maven-settings.xml spring-boot:run` 已在 `18080` 端口启动成功；`/actuator/health` 返回 `{"status":"UP"}`；MCP 自动注册 `29` 个工具。
- [x] 完成后台模型配置中心真实配置与保存
  - 验收：已通过 Admin 接口从空库创建 `1` 条连接、`3` 条模型、`5` 条绑定，覆盖 `compile.writer / compile.reviewer / compile.fixer / query.answer / query.rewrite`，且 compile / query 运行时快照均已实际冻结。
- [x] 完成复杂知识源全量编译与增量编译验收
  - 验收：全量编译对外 `jobId=36e48223-0d63-43ab-a39f-52b43f3c7b38`，结果 `SUCCEEDED`，`persistedCount=9`；Graph 侧真实跑过 `compile -> review -> fix -> review -> persist -> snapshot` 完整链路。
  - 验收：增量编译对外 `jobId=97fd3b1c-b21d-4de7-93a5-47f8f27dc82f`，结果 `SUCCEEDED`，修改 `ops/postmortem.md` 后 Query 已能答出新值 `50 / 20 分钟`。
  - 发现：当前增量模式可用，但一次单文件修改后仍 `persistedCount=9`，持久化粒度偏粗。
- [x] 完成复杂查询、纠错、确认、丢弃全链路验收
  - 验收：已真实完成配置阈值、ADR 原因、PDF 口令手册、Excel 模板、Runbook 指标等问题的问答。
  - 验收：已真实执行 `correct -> confirm`、直接 `confirm`、`discard`，最终 `contributionCount=2`，`pendingQueryCount=0`。
- [x] 完成 Admin 侧核心功能验收
  - 验收：已真实调用 `overview / articles / pending / quality / coverage / omissions / lint / snapshot/article / snapshot/repo / rollback/article / vault/export`。
  - 验收：`integrations` 已完成 `deprecate -> activate` 生命周期切换；`ops` 已完成文章级 `rollback -> rollback back` 双向回滚验证。
- [x] 完成 MCP 工具主链路验收
  - 验收：已用官方 MCP Java client 直连 `http://127.0.0.1:18080/mcp`，成功执行 `initialize / tools/list / lattice_ping / lattice_status / lattice_query / lattice_snapshot / lattice_query_pending / lattice_query_discard`。
- [x] 完成导出、快照、回滚等高阶能力抽检
  - 验收：Vault 导出成功，目录 `/tmp/lattice-real-e2e-vault` 已生成 `concepts/*.md`、`_meta/export-manifest.json`、`_contributions/*.md`。
  - 验收：`repo_snapshots=2`，文章级 `article_snapshots=24`，文章级回滚能力已真实生效。
  - 边界：整库 repo diff / repo rollback 依赖 Git-backed vault baseline；本轮仅完成 history 验证，未执行 diff / rollback。
- [x] 形成详细项目手册并更新 `README.md`
  - 验收：已新增 `docs/项目全流程真实验收手册.md`，并重写 `README.md` 当前入口、单一职责定位、快速启动与已知问题说明。
- [x] 完成文档自检与最终口径修正
  - 验收：已再次校验 `README.md`、`docs/项目全流程真实验收手册.md` 与本清单；确认验收实例当前仍可通过 `http://127.0.0.1:18080/actuator/health` 与 `http://127.0.0.1:18080/api/v1/admin/overview` 返回正常结果，并统一了 README 中的端口与 Admin 入口说明。

## 本轮发现

- [x] 记录向量索引降级问题
  - 结论：当前 `t1.md` 网关能处理 chat，但 embeddings 接口返回 `404`，导致 `article_vector_index=0`；向量索引和向量检索都走了 fail-open 降级。
- [x] 记录 Admin 文章纠错接口失败问题
  - 结论：`POST /api/v1/admin/articles/security/correct` 在当前网关下返回 `500`；服务端根因是 `HTTP 400 - Request body is empty`，本轮未修复。
- [x] 记录 compile job 观测链路问题
  - 结论：`compile_job_steps.job_id` 与对外 `compile_jobs.job_id` 当前不是同一个标识，运维排查时不能直接用对外 jobId 关联步骤日志。
- [x] 记录二次编译后的人审状态变化
  - 结论：本轮最终 `payment-gateway-error-codes` 与 `payment-timeout-and-retry-policy` 为 `needs_human_review`，`fulfillment` 已恢复为 `passed`。
