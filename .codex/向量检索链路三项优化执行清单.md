# 向量检索链路三项优化执行清单

> 唯一进度台账：本轮“按《向量检索链路三项优化技术方案》落地 embedding 配置中心化、schema 强约束、chunk 向量与 Query 图并行召回”统一回写到本文件。

## 验收目标

- [x] 完成基线 schema 与向量配置持久化模型收口
  - 验收：`V1__baseline_schema.sql` 已引入 `llm_model_profiles.model_kind / expected_dimensions / supports_dimension_override`、`query_vector_settings.embedding_model_profile_id`、`query_retrieval_settings`、`article_chunk_vector_index`，并清理旧 `V2__query_vector_settings.sql` 过渡语义。
- [x] 完成 Embedding Provider 统一路由基础设施
  - 验收：已新增 `EmbeddingRouteResolver / EmbeddingClientFactory / EmbeddingRouteResolution / EmbeddingProfileChangedEvent`，`ConfiguredVectorEmbeddingService` 不再依赖单一全局 `EmbeddingModel` Bean。
- [x] 完成向量配置后台接口与运行时配置改造
  - 验收：`/api/v1/admin/vector/config` 与后台页面已改为保存 `embeddingModelProfileId`，可展示 provider、model、profile 维度，并在 profile 切换后触发本地 cache 失效。
- [x] 完成 Vector schema 强约束与状态探测
  - 验收：已新增 `VectorSchemaInspector`，`GET /api/v1/admin/vector/status` 可返回 `profileDimensions / schemaDimensions / dimensionsConsistent / annIndexReady / annIndexType`。
- [x] 完成文章级向量索引结构重写
  - 验收：`article_vector_index` 已改为按 `model_profile_id + embedding_dimensions + index_version + content_hash` 落库，文章级索引服务与重建链路已同步收口。
- [x] 完成 chunk 级向量索引与编译链路接线
  - 验收：已新增 `article_chunk_vector_index`、`ArticleChunkVectorIndexService` 与对应仓储，编译/重建链路会同时刷新文章级和 chunk 级向量索引。
- [x] 完成 Query 图并行召回与加权 RRF 融合
  - 验收：`QueryGraphDefinitionFactory` 已拆成 `FTS / RefKey / Source / Contribution / Article Vector / Chunk Vector` 六路并行召回，`RrfFusionService` 已按 `query_retrieval_settings` 执行加权 RRF。
- [x] 完成自动化测试与针对性回归
  - 验收：新增并通过 `EmbeddingRouteResolverTest`、`ConfiguredVectorEmbeddingServiceTest`、`VectorSchemaInspectorTest`、`ChunkToArticleAggregatorTest`、`WeightedRrfFusionTest` 及相关集成测试。
- [x] 完成 Query 检索配置后台可编辑页面
  - 验收：后台已支持展示并保存 `parallelEnabled / ftsWeight / sourceWeight / contributionWeight / articleVectorWeight / chunkVectorWeight / rrfK`，修改后可实时影响加权 RRF 与并行召回配置。
- [x] 完成真实全量链路验收
  - 验收：已基于 `.claude/t1.md` 的真实配置完成全量编译、文章级与 chunk 级向量重建、HTTP / CLI / MCP 复杂 query 回归，并记录关键结果与问题。

## 执行记录

- [x] 完成基线 schema 与向量配置持久化模型收口
  - 已完成：`V1__baseline_schema.sql` 已收口到单基线，补齐 `llm_model_profiles` 向量字段、`query_vector_settings.embedding_model_profile_id`、`query_retrieval_settings`、`article_vector_index` 新结构与 `article_chunk_vector_index`，并删除旧 `V2__query_vector_settings.sql`。
- [x] 完成 Embedding Provider 统一路由基础设施
  - 已完成：已新增 `EmbeddingRouteResolver / EmbeddingClientFactory / EmbeddingRouteResolution / EmbeddingProfileChangedEvent`，`ConfiguredVectorEmbeddingService` 生产路径已改为按 `embeddingModelProfileId -> provider connection -> runtime client` 路由，profile 切换后会清空本地 embedding client cache。
  - 验收：已通过 `mvn -q -s .codex/maven-settings.xml -Dmaven.repo.local=/Users/sxie/maven/repository -Dtest=AdminVectorConfigControllerTests,LlmConfigCenterIntegrationTests,ArticleVectorIndexServiceTests,VectorSearchServiceTests,ExecutionLlmSnapshotServiceTests clean test`。
- [x] 完成向量配置后台接口与运行时配置改造
  - 已完成：`/api/v1/admin/vector/config`、admin 页面脚本与模型中心后台页已切到 `embeddingModelProfileId`，同时补齐了 `EMBEDDING` 模型类型、期望维度和 provider 展示逻辑。
- [x] 完成 Vector schema 强约束与状态探测
  - 已完成：已新增 `VectorSchemaInspector`，`GET /api/v1/admin/vector/status` 现可返回 `embeddingModelProfileId / profileDimensions / schemaDimensions / dimensionsConsistent / annIndexReady / annIndexType`，并在维度不一致时阻止重建。
- [x] 完成文章级向量索引结构重写
  - 已完成：`article_vector_index` 已改为按 `model_profile_id + embedding_dimensions + index_version + content_hash` 落库；`ArticleVectorJdbcRepository / ArticleVectorIndexService` 与 admin 状态/重建链路已同步切换。
- [x] 完成 chunk 级向量索引与编译链路接线
  - 已完成：已新增 `ArticleChunkVectorJdbcRepository / ArticleChunkVectorIndexService / ChunkVectorSearchService`，compile full、incremental 和 admin rebuild 都会真正写入 `article_chunk_vector_index`。
- [x] 完成 Query 图并行召回与加权 RRF 融合
  - 已完成：已新增 `QueryRetrievalSettingsService`、按 channel 的 working set 命中引用、`ChunkToArticleAggregator`，并把 Query Graph 拆成 `dispatch_retrieval -> 6 路并行召回 -> fuse_candidates`。
- [x] 完成自动化测试与针对性回归
  - 已完成：已新增并通过 `EmbeddingRouteResolverTest`、`ConfiguredVectorEmbeddingServiceTest`、`VectorSchemaInspectorTest`、`ChunkToArticleAggregatorTest`、`WeightedRrfFusionTest`。
  - 已完成：已通过 `AdminVectorConfigControllerTests`、`AdminVectorIndexControllerTests`、`LlmConfigCenterIntegrationTests`、`ArticleVectorIndexServiceTests`、`VectorSearchServiceTests`、`ExecutionLlmSnapshotServiceTests`、`CompilePipelineVectorIndexingTests`、`QueryGraphOrchestratorTests`、`QueryFacadeServiceVectorTests`。
  - 已完成：已把 `SiliconFlow /v1/embeddings + BAAI/bge-m3` 的真实验证样例补充到 [`.claude/t1.md`](/Users/sxie/xbk/Lattice-java/.claude/t1.md)，当前实测 `HTTP 200`、向量维度 `1024`，可作为后续 embedding 真实验收输入。
- [x] 完成 Query 检索配置后台可编辑页面
  - 已完成：已新增 `AdminQueryRetrievalConfigController`、`AdminQueryRetrievalConfigRequest / Response`、`QueryRetrievalSettingsService.save(...)` 与 `QueryRetrievalSettingsJdbcRepository.saveDefault(...)`，后台页面可直接加载和保存检索融合配置。
  - 验收：已通过 `AdminQueryRetrievalConfigControllerTests`、`AdminPageControllerTests`、`QueryGraphOrchestratorTests`、`QueryFacadeServiceVectorTests`、`WeightedRrfFusionTest`。
- [x] 完成真实全量链路验收
  - 已完成：基于 `.claude/t1.md` 的真实配置重启 `18082` 实例，继续复用独立 schema `lattice_real_e2e_20260418_sf1024`；`GET /api/v1/admin/vector/status` 返回 `configuredExpectedDimensions=1024 / schemaDimensions=1024 / indexedArticleCount=9`，配置维度与 schema 维度一致。
  - 已完成：`ArticleVectorJdbcRepository / ArticleChunkVectorJdbcRepository` 已按 vector 类型 schema 显式解析 pgvector 距离运算符，避免独立 schema 连接下 `<=>` 无法解析到 `public` 扩展运算符。
  - 验收：已新增并通过 `VectorJdbcRepositoryOperatorTests`，确认 `currentSchema=独立 schema` 且向量列为 `public.vector` 时，文章级与 chunk 级近邻检索均可正常返回结果。
  - 验收：已通过 `AdminVectorIndexControllerTests`、`AdminVectorConfigControllerTests`、`ConfiguredVectorEmbeddingServiceTest`、`AdminQueryRetrievalConfigControllerTests`、`QueryGraphOrchestratorTests`、`QueryFacadeServiceVectorTests`。
  - 验收：真实 `POST /api/v1/admin/vector/rebuild` 返回 `targetArticleCount=9 / indexedArticleCount=9 / indexedChunkCount=9 / configuredModelName=BAAI/bge-m3`。
  - 验收：真实 HTTP 复杂 query 已返回结构化答案；服务日志中 `retrieve_article_vector / retrieve_chunk_vector` 均完成，且不再出现 `operator does not exist: public.vector <=> public.vector`。
  - 验收：CLI remote 已真实跑通 `status / search / query / vault-export`；其中 `search` 返回 `count=5`，`vault-export` 返回 `writtenFiles=13`。
  - 验收：MCP raw HTTP 已真实跑通 `initialize / tools/list / tools/call(lattice_status) / tools/call(lattice_query)`；`tools/list` 返回 29 个工具，`lattice_query` 返回 `queryId / reviewStatus / sourceCount`。
