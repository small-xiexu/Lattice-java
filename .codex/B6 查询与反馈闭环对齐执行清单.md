# B6 查询与反馈闭环对齐执行清单

> **唯一进度台账**：自本文件创建起，B6“查询与反馈闭环对齐”阶段的执行进度、进行中事项、阻塞信息、验收依据统一只回写到本文件，不再分散记录到其他文档。

**目标**：对齐原版“文章层 + refkey + 源文件层 + contribution 层”的完整问答闭环，补齐多路检索、LLM 结构化回答、纠错重生成与 MCP 查询反馈工具集。

**范围边界**：

- 本清单只覆盖 B6，不包含 B7 治理/MCP 全工具集、B8 Java 增强项
- 本阶段优先补主查询链与反馈闭环，不引入 Web 后台
- 本阶段可继续复用 PostgreSQL / Redis / Spring AI 现有基线

**设计约束**：

- 查询主链必须同时包含 article / refkey / source / contribution 四层证据
- `correct(...)` 不再只是字符串拼接，需具备基于纠正重生成答案的能力
- B6 所有完成勾选必须有代码或测试依据

---

## 执行顺序

`Task 1 -> Task 2 -> Task 3 -> Task 4`

说明：

- Task 1 是 B6 主链硬前置，先补 source/contribution 检索与 LLM answer generation
- Task 2 依赖 Task 1 的查询上下文结构稳定
- Task 3 依赖 Task 2 的 pending / contribution 语义稳定
- Task 4 依赖 Task 1-3 的查询反馈链路收口

---

### Task 1：多路检索主链与 LLM 回答基础层

**目标**：让查询主链从“文章层 + refkey”的最小闭环，升级为“文章层 + refkey + 源文件层 + contribution 层”的多路证据检索，并由 LLM 生成 Markdown 答案。

- 已完成：`source_file_chunks` 建表、编译阶段 source chunk 落盘、source/contribution 检索、四路 RRF 融合与 `AnswerGenerationService` 的 LLM Markdown 生成/降级兜底已落地
- 验收：`mvn -s .codex/maven-settings.xml -Dtest=QueryControllerTests,AnswerGenerationServiceTests test` 通过；补充回归 `mvn -s .codex/maven-settings.xml -Dtest=IncrementalCompileServiceTests,CompilePipelineWalRecoveryTests,QueryFacadeServiceCacheTests test` 通过

**Files**

- Modify: `src/main/java/com/xbk/lattice/query/service/QueryFacadeService.java`
- Modify: `src/main/java/com/xbk/lattice/query/service/AnswerGenerationService.java`
- Modify: `src/main/java/com/xbk/lattice/query/service/RrfFusionService.java`
- Modify: `src/main/java/com/xbk/lattice/compiler/service/CompilePipelineService.java`
- Modify: `src/main/java/com/xbk/lattice/compiler/service/IncrementalCompileService.java`
- Modify: `src/main/java/com/xbk/lattice/infra/persistence/ContributionJdbcRepository.java`
- Create: `src/main/java/com/xbk/lattice/query/service/SourceSearchService.java`
- Create: `src/main/java/com/xbk/lattice/query/service/ContributionSearchService.java`
- Create: `src/main/java/com/xbk/lattice/infra/persistence/SourceFileChunkJdbcRepository.java`
- Create: `src/main/java/com/xbk/lattice/infra/persistence/SourceFileChunkRecord.java`
- Create: `src/main/resources/db/migration/V10__create_source_file_chunks.sql`
- Test: `src/test/java/com/xbk/lattice/api/query/QueryControllerTests.java`
- Test: `src/test/java/com/xbk/lattice/query/service/AnswerGenerationServiceTests.java`

- [x] 为 `source_file_chunks` 建表并补齐编译阶段分块落盘
- [x] 新增 `SourceSearchService`，支持 source chunk 检索
- [x] 新增 `ContributionSearchService`，支持 contribution 检索
- [x] 将 `QueryFacadeService` 改为融合四层证据，而不是只融合 article/refkey
- [x] 将 `AnswerGenerationService` 改为基于多路上下文调用 `LlmGateway` 生成 Markdown 答案
- [x] 补齐查询主链与答案生成测试
- [x] 验收：`mvn -s .codex/maven-settings.xml -Dtest=QueryControllerTests,AnswerGenerationServiceTests test`

### Task 2：Pending / Correct / Confirm / Discard 闭环重做

**目标**：让纠错不再只是字符串追加，而是基于纠正信息重生成答案，并保留 confirm / discard 的完整闭环。

- 已完成：`correct(...)` 已切为基于纠正信息与 article/source/contribution 证据重生成答案；corrections JSON 已记录版本与修订前后答案；`confirm/discard` 闭环已按状态语义收口
- 验收：`mvn -s .codex/maven-settings.xml -Dtest=PendingQueryControllerTests test` 通过；联合验收 `mvn -s .codex/maven-settings.xml -Dtest=QueryControllerTests,PendingQueryControllerTests,LatticeMcpToolsTest test` 通过

**Files**

- Modify: `src/main/java/com/xbk/lattice/query/service/PendingQueryService.java`
- Modify: `src/main/java/com/xbk/lattice/api/query/PendingQueryController.java`
- Modify: `src/main/java/com/xbk/lattice/query/service/PendingQueryManager.java`
- Modify: `src/main/java/com/xbk/lattice/infra/persistence/PendingQueryJdbcRepository.java`
- Test: `src/test/java/com/xbk/lattice/api/query/PendingQueryControllerTests.java`

- [x] 将 `correct(...)` 改为基于纠正重生成答案
- [x] 保留 correction history，并记录修订后的答案版本
- [x] `confirm(...)` 落 contribution，`discard(...)` 标准结束
- [x] 补齐 pending 控制器测试
- [x] 验收：`mvn -s .codex/maven-settings.xml -Dtest=PendingQueryControllerTests test`

### Task 3：Contribution 感知查询

**目标**：让确认后的贡献内容可被后续查询命中，并作为高优先级证据参与回答。

- 已完成：contribution 检索与回答上下文已接通；确认后的 contribution 可作为后续查询证据命中，并在回答证据排序中优先参与生成
- 验收：`mvn -s .codex/maven-settings.xml -Dtest=QueryControllerTests test` 通过；联合验收 `mvn -s .codex/maven-settings.xml -Dtest=QueryControllerTests,PendingQueryControllerTests,LatticeMcpToolsTest test` 通过

**Files**

- Modify: `src/main/java/com/xbk/lattice/infra/persistence/ContributionJdbcRepository.java`
- Modify: `src/main/java/com/xbk/lattice/query/service/QueryFacadeService.java`
- Modify: `src/main/java/com/xbk/lattice/query/service/ContributionSearchService.java`
- Test: `src/test/java/com/xbk/lattice/api/query/QueryControllerTests.java`

- [x] 为 contribution 检索补查询能力
- [x] 将 contribution 命中接入回答上下文
- [x] 验证已确认 contribution 可在后续问题中命中
- [x] 验收：`mvn -s .codex/maven-settings.xml -Dtest=QueryControllerTests test`

### Task 4：MCP 查询反馈工具集对齐

**目标**：补齐 `lattice_query_pending` 列表语义与 `lattice_query_discard` 工具，使 MCP 查询反馈链路对齐原版。

- 已完成：`lattice_query_pending` 已改为返回全部 pending 列表；`lattice_query_discard` 已补齐，并与 pending 状态结构对齐
- 验收：`mvn -s .codex/maven-settings.xml -Dtest=LatticeMcpToolsTest test` 通过；联合验收 `mvn -s .codex/maven-settings.xml -Dtest=QueryControllerTests,PendingQueryControllerTests,LatticeMcpToolsTest test` 通过

**Files**

- Modify: `src/main/java/com/xbk/lattice/mcp/LatticeMcpTools.java`
- Modify: `src/main/java/com/xbk/lattice/query/service/PendingQueryManager.java`
- Modify: `src/main/java/com/xbk/lattice/query/service/PendingQueryService.java`
- Modify: `src/main/java/com/xbk/lattice/infra/persistence/PendingQueryJdbcRepository.java`
- Test: `src/test/java/com/xbk/lattice/mcp/LatticeMcpToolsTest.java`

- [x] `lattice_query_pending` 改为列出全部 pending
- [x] 补齐 `lattice_query_discard`
- [x] 校准 MCP 工具返回结构与状态语义
- [x] 验收：`mvn -s .codex/maven-settings.xml -Dtest=LatticeMcpToolsTest test`

---

## 验收汇总

- [x] Task 1 全部完成并通过验证
- [x] Task 2 全部完成并通过验证
- [x] Task 3 全部完成并通过验证
- [x] Task 4 全部完成并通过验证

---

## 断点区

- 当前状态：B6 已完成
- 已完成：Task 1-4 已全部落地，B6 联合验收 `mvn -s .codex/maven-settings.xml -Dtest=QueryControllerTests,PendingQueryControllerTests,LatticeMcpToolsTest test` 与相关回归 `mvn -s .codex/maven-settings.xml -Dtest=IncrementalCompileServiceTests,CompilePipelineWalRecoveryTests,QueryFacadeServiceCacheTests test` 均已通过
- 下一步：进入后续阶段前，按新台账继续选择 B7 或收口整理
