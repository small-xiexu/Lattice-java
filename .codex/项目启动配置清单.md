# Lattice Java 项目启动配置过程

## 1. 这份文档要回答什么

这份文档不是“打勾式清单”，而是直接回答一个问题：

> 这个项目真正启动起来之前，我到底需要配置什么，按什么顺序做？

适用范围：

- 当前仓库本地开发环境
- Spring Boot 3.5.1
- JDK 21
- PostgreSQL 16
- Redis 7

基线来源：

- `docker-compose.yml`
- `src/main/resources/application.yml`
- `src/main/resources/db/migration/V1__baseline_schema.sql`
- `pom.xml`

---

## 2. 一句话结论

如果你按仓库默认口径本地启动，这个项目真正需要你处理的事情只有 4 件：

1. 准备好 `JDK 21`、`Maven`、`Docker`
2. 拉起 `PostgreSQL` 和 `Redis`
3. 在数据库里创建业务 schema：`lattice`
4. 用 `jdbc` profile 启动 Spring Boot

其他配置大多都有默认值，不是启动阻塞项。

---

## 3. 项目真正需要配置的东西

下面按“是否会阻塞启动”来分。

### 3.1 启动必配项

| 项目 | 是否必须显式处理 | 当前口径 |
| --- | --- | --- |
| JDK | 是 | 必须是 `JDK 21` |
| PostgreSQL | 是 | 默认连接 `127.0.0.1:5432/ai-rag-knowledge` |
| Redis | 是 | 默认连接 `127.0.0.1:6379` |
| Spring Profile | 是 | 以 `jdbc` 启动 |
| Flyway schema | 是 | 业务 schema 固定为 `lattice` |
| `lattice` schema 创建 | 是 | 需要提前建好，否则 Flyway 无法按预期落到业务 schema |

### 3.2 有默认值、可不显式配置的项

| 项目 | 默认值 | 什么时候才需要自己改 |
| --- | --- | --- |
| `SPRING_DATASOURCE_URL` | `jdbc:postgresql://127.0.0.1:5432/ai-rag-knowledge?currentSchema=lattice` | 你的数据库地址、库名、schema 不是默认值时 |
| `SPRING_DATASOURCE_USERNAME` | `postgres` | 账号不是 `postgres` 时 |
| `SPRING_DATASOURCE_PASSWORD` | `postgres` | 密码不是 `postgres` 时 |
| `SPRING_FLYWAY_ENABLED` | `true` | 你要临时关闭 Flyway 时 |
| `SPRING_FLYWAY_SCHEMAS` | `lattice` | 你要迁到别的 schema 时 |
| `SPRING_FLYWAY_DEFAULT_SCHEMA` | `lattice` | 你要迁到别的 schema 时 |
| `LATTICE_REDIS_HOST` | `127.0.0.1` | Redis 不在本机时 |
| `LATTICE_REDIS_PORT` | `6379` | Redis 端口不是 `6379` 时 |
| `SPRING_AI_OPENAI_API_KEY` | `any-non-empty-value` | 你要真实调用 OpenAI 时 |
| `SPRING_AI_OPENAI_BASE_URL` | `https://api.openai.com` | 你使用 OpenAI 兼容网关、中转平台或公司代理时 |
| `SPRING_AI_ANTHROPIC_API_KEY` | `any-non-empty-value` | 你要真实调用 Anthropic 时 |
| `SPRING_AI_ANTHROPIC_BASE_URL` | `https://api.anthropic.com` | 你使用 Anthropic 兼容网关、中转平台或公司代理时 |

补充口径：

- 从 Spring AI 能力上看，模型 URL 已经可以纯走配置，不需要改业务代码
- 但考虑到你这里的实际使用习惯通常是“自定义模型 URL”，所以建议把 `base-url` 也当成常规显式配置项
- 如果你直连官方模型服务，可以不配；如果你走 88code、oneapi 或其他兼容网关，就必须显式配置

### 3.3 可选增强项

这些不是启动前置条件，只是增强能力：

- `LATTICE_LLM_REVIEW_ENABLED=true`
  - 开启真实 LLM 审查
- `LATTICE_QUERY_SEARCH_VECTOR_ENABLED=true`
  - 开启向量检索
- `LATTICE_QUERY_SEARCH_FTS_CONFIG`
  - 指定全文检索首选 `tsconfig`

---

## 4. 推荐启动方式

对当前项目，最推荐的本地启动方式是：

1. 用仓库自带 `docker-compose.yml` 拉起 PostgreSQL 和 Redis
2. 手工创建一次 `lattice` schema
3. 用 `mvn -s .codex/maven-settings.xml spring-boot:run` 启动

这样最贴近当前仓库的真实开发口径。

---

## 5. 从零启动的实际操作过程

下面是按顺序执行的一套流程。

### 第 1 步：确认基础环境

先确认这 3 个东西都可用：

- `java` 指向 `JDK 21`
- `mvn` 可用
- `docker compose` 可用

建议执行：

```bash
java -version
mvn -version
docker compose version
```

你还可以顺手确认默认端口没被占：

```bash
lsof -i :5432
lsof -i :6379
lsof -i :8080
```

如果 `5432` 或 `6379` 已经被别的实例占用了，就要么停掉旧实例，要么改你自己的连接配置。

### 第 2 步：拉起依赖服务

项目根目录执行：

```bash
docker compose up -d postgres redis
docker compose ps
```

正常情况下你应该看到：

- `lattice_b1_postgres`
- `lattice_b3_redis`

状态最终变成 `healthy`。

### 第 3 步：创建业务 schema

这个动作要做一次：

```bash
docker exec lattice_b1_postgres psql -U postgres -d ai-rag-knowledge \
  -c "CREATE SCHEMA IF NOT EXISTS lattice;"
```

为什么这一步要单独做：

- 当前项目默认业务 schema 是 `lattice`
- Flyway 默认也往 `lattice` 迁移
- 所以你至少要让这个 schema 存在

### 第 4 步：决定要不要显式导出环境变量

如果你完全使用仓库默认口径，本地其实可以不显式导出大部分变量。

但为了避免串环境，建议至少导出下面这组：

```bash
export SPRING_PROFILES_ACTIVE=jdbc
export SPRING_DATASOURCE_URL="jdbc:postgresql://127.0.0.1:5432/ai-rag-knowledge?currentSchema=lattice"
export SPRING_DATASOURCE_USERNAME=postgres
export SPRING_DATASOURCE_PASSWORD=postgres
export SPRING_FLYWAY_ENABLED=true
export SPRING_FLYWAY_SCHEMAS=lattice
export SPRING_FLYWAY_DEFAULT_SCHEMA=lattice
export LATTICE_REDIS_HOST=127.0.0.1
export LATTICE_REDIS_PORT=6379
```

关于 API Key：

- 当前 `application.yml` 已给了非空默认值
- 所以“只为启动成功”这件事，并不要求你现在就配真实 OpenAI / Anthropic Key
- 只有你要跑真实模型调用时，才需要改成有效 Key

如果你要走真实模型，而且通常使用自定义网关，建议把 Key 和 URL 一起显式导出：

```bash
export SPRING_AI_OPENAI_API_KEY=你的真实 OpenAI Key
export SPRING_AI_OPENAI_BASE_URL=你的 OpenAI 兼容网关地址
export SPRING_AI_ANTHROPIC_API_KEY=你的真实 Anthropic Key
export SPRING_AI_ANTHROPIC_BASE_URL=你的 Anthropic 兼容网关地址
```

如果你只是本地验证启动，不打真实模型调用，也可以继续使用默认占位值和默认官方 URL，不会阻塞服务启动。

### 第 5 步：如果你本地跑过旧迁移，先做清理

这是当前项目最容易踩坑的地方。

如果你本地之前跑过旧版 `V1` 到 `V15` 多文件迁移，当前版本已经改成单基线 `V1__baseline_schema.sql`，这时要先清理旧状态。

最少要注意两件事：

1. 清理旧的编译产物，避免 `target/classes/db/migration` 里残留旧脚本
2. 清理旧 schema 或重建数据库，避免 Flyway 历史与当前基线不一致

建议执行：

```bash
mvn clean
```

如果你确定本地 `lattice` schema 是旧版本打出来的，建议直接删掉重建：

```bash
docker exec lattice_b1_postgres psql -U postgres -d ai-rag-knowledge \
  -c "DROP SCHEMA IF EXISTS lattice CASCADE; CREATE SCHEMA lattice;"
```

### 第 6 步：启动应用

当前推荐命令：

```bash
mvn -s .codex/maven-settings.xml spring-boot:run
```

如果你全局 Maven 镜像已经稳定，也可以直接：

```bash
mvn spring-boot:run
```

如果你更想跑打包产物：

```bash
mvn -s .codex/maven-settings.xml package -DskipTests
java -jar target/lattice-java-1.0-SNAPSHOT.jar
```

### 第 7 步：看启动成功没有

启动成功至少要满足 3 件事：

1. 日志出现 `Started LatticeApplication`
2. Flyway 没报错
3. 健康检查是 `UP`

建议执行：

```bash
curl http://127.0.0.1:8080/actuator/health
docker exec lattice_b1_postgres psql -U postgres -d ai-rag-knowledge -c "\dt lattice.*"
```

如果返回类似下面内容，说明基础启动已经成功：

```json
{"status":"UP"}
```

---

## 6. 你真正要改配置的场景

下面按场景说明“什么时候才需要改配置”。

### 场景 A：只是本地把服务跑起来

你真正要配的是：

- JDK 21
- PostgreSQL
- Redis
- `lattice` schema
- `jdbc` profile

其他大多数配置可以沿用默认值。

### 场景 B：你要真实调用 OpenAI / Anthropic

这时建议把 Key 和 URL 一起明确下来：

```bash
export SPRING_AI_OPENAI_API_KEY=你的真实 OpenAI Key
export SPRING_AI_OPENAI_BASE_URL=你的 OpenAI 兼容网关地址
export SPRING_AI_ANTHROPIC_API_KEY=你的真实 Anthropic Key
export SPRING_AI_ANTHROPIC_BASE_URL=你的 Anthropic 兼容网关地址
```

说明：

- 如果你直连官方服务，`SPRING_AI_OPENAI_BASE_URL` 可用默认值 `https://api.openai.com`
- 如果你直连官方服务，`SPRING_AI_ANTHROPIC_BASE_URL` 可用默认值 `https://api.anthropic.com`
- 如果你走兼容平台，这两个 URL 一般都要改

如果你不开真实模型调用，这几项都不是启动阻塞项。

### 场景 C：你要启用向量检索

这时需要额外处理两件事：

1. 数据库里存在 `vector` 类型
2. 显式开启向量检索

建议补：

```bash
docker exec lattice_b1_postgres psql -U postgres -d ai-rag-knowledge \
  -c "CREATE EXTENSION IF NOT EXISTS vector;"

export LATTICE_QUERY_SEARCH_VECTOR_ENABLED=true
```

说明：

- 当前基线脚本会在检测到 `vector` 类型存在时，才创建 `article_vector_index`
- 所以向量检索不是默认强制项

### 场景 D：你要指定全文检索配置

当前默认：

- 首选：`jiebacfg`
- 回退：`simple`

只有当你明确知道数据库里没有 `jiebacfg`，或者你要换自己的分词配置时，才需要改：

```bash
export LATTICE_QUERY_SEARCH_FTS_CONFIG=simple
export LATTICE_QUERY_SEARCH_FTS_FALLBACK=simple
```

---

## 7. 一套可以直接照抄的本地启动命令

如果你的目标只是“把项目在本机跑起来”，可以直接按下面执行：

```bash
cd /Users/sxie/xbk/Lattice-java

docker compose up -d postgres redis

docker exec lattice_b1_postgres psql -U postgres -d ai-rag-knowledge \
  -c "CREATE SCHEMA IF NOT EXISTS lattice;"

export SPRING_PROFILES_ACTIVE=jdbc
export SPRING_DATASOURCE_URL="jdbc:postgresql://127.0.0.1:5432/ai-rag-knowledge?currentSchema=lattice"
export SPRING_DATASOURCE_USERNAME=postgres
export SPRING_DATASOURCE_PASSWORD=postgres
export SPRING_FLYWAY_ENABLED=true
export SPRING_FLYWAY_SCHEMAS=lattice
export SPRING_FLYWAY_DEFAULT_SCHEMA=lattice
export LATTICE_REDIS_HOST=127.0.0.1
export LATTICE_REDIS_PORT=6379
export SPRING_AI_OPENAI_BASE_URL=https://api.openai.com
export SPRING_AI_ANTHROPIC_BASE_URL=https://api.anthropic.com

mvn -s .codex/maven-settings.xml spring-boot:run
```

启动后再执行：

```bash
curl http://127.0.0.1:8080/actuator/health
```

---

## 8. 常见启动失败原因

### 8.1 还是在用旧迁移结果

现象：

- Flyway checksum 不一致
- 提示同版本脚本冲突
- 明明仓库只有一个 `V1`，启动却像看到了很多旧迁移

原因：

- 旧版 `V1` 到 `V15` 的编译产物或 schema 历史还在

处理：

```bash
mvn clean
```

必要时重建 schema：

```bash
docker exec lattice_b1_postgres psql -U postgres -d ai-rag-knowledge \
  -c "DROP SCHEMA IF EXISTS lattice CASCADE; CREATE SCHEMA lattice;"
```

### 8.2 数据库能连上，但业务表没建出来

原因通常是：

- `currentSchema` 不是 `lattice`
- `SPRING_FLYWAY_SCHEMAS` / `SPRING_FLYWAY_DEFAULT_SCHEMA` 配错
- `lattice` schema 根本没创建

### 8.3 Redis 连接失败

优先检查：

- `docker compose ps`
- `LATTICE_REDIS_HOST`
- `LATTICE_REDIS_PORT`

### 8.4 真实 LLM 调用失败

如果你已经切到真实 Key，仍然失败，优先检查：

- Key 是否有效
- 当前代理 / 网络是否能访问模型服务
- 你是否真的需要开启真实调用，而不是只做本地启动验证

---

## 9. 最终建议

对当前仓库，最实用的启动口径是：

1. 先把 PostgreSQL 和 Redis 跑起来
2. 创建 `lattice` schema
3. 必要时执行一次 `mvn clean`
4. 用 `mvn -s .codex/maven-settings.xml spring-boot:run` 启动
5. 用 `/actuator/health` 和 `\dt lattice.*` 做最小验收

如果后面你愿意，我可以继续把这份文档再拆成两版：

- 面向新同学的一键启动版
- 面向生产/测试环境的变量模板版
