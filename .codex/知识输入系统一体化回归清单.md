# 知识输入系统一体化回归清单

## 固定回归入口

- 脚本：[`scripts/smoke-knowledge-input.sh`](/Users/sxie/xbk/Lattice-java/scripts/smoke-knowledge-input.sh)
- Maven Settings：[`/.codex/maven-settings.xml`](/Users/sxie/xbk/Lattice-java/.codex/maven-settings.xml)
- Maven 本地仓库：`/Users/sxie/maven/repository`

## 推荐执行顺序

1. 运行 `./scripts/smoke-knowledge-input.sh`
2. 打开 `/admin`
3. 验证“上传资料”“服务器目录资料源”“最近同步运行”“资料源与文件”“已入库内容”五个区块都能正常加载
4. 打开 `/admin/ai`
5. 验证“文档解析连接 / 文档解析设置 / 资料源凭据列表”都能正常加载
6. 打开 `/admin/ask`
7. 验证在存在活动 `source-runs` 或 `WAIT_CONFIRM` 时，页面提示会同步变化

## 关键观察点

- 上传后优先看到 `source-runs` 状态，而不是只看 compile job
- 多资料源场景下，文章详情应按 `articleKey + sourceId` 读取，不应因重复 `conceptId` 误命中
- 资料源详情页应能看到 `parseMode / parseProvider`
- AI 页资料源凭据列表只返回脱敏值，不显示明文 secret
- CLI 根帮助中应出现 `source-list`、`source-sync`
- MCP 工具中应存在 `lattice_source_list`、`lattice_source_sync`
